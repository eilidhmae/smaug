# Changelog

## 2026-04-19

- **Phase 6 Holidays landed — `holidays` / `saveholiday` / `setholiday` + `holidays.dat` loader/saver + `month_name[]` port + `DoTime` holiday-today suffix + `GetHoliday` helper** (`plan-phase6-holidays.md`). 4 task groups (G1 types + `MonthName`, G2 loader/saver + testdata fixtures, G3 3 commands + `GetHoliday` + `DoTime` suffix, G4 boot wire + `SysData.MaxHoliday`/`MonthsPerYear`/`DaysPerMonth` defaults + 3 command registrations), 15 acceptance criteria all satisfied (A1–A13 plus A14/A15 from the 2026-04-19 readiness vet). New files: `internal/types/months.go` (40 LOC, `MonthNames [17]string` verbatim from `src/act_info.c:2455-2460` + `MonthName(m)` helper with OOB→`"<unknown>"` fallback); `internal/types/holiday.go` (14 LOC, `HolidayData{Month, Day int; Name, Announce string}` — 1-indexed in memory to match file format); `internal/types/months_test.go` (7 tests); `internal/persist/holidays.go` (168 LOC, `LoadHolidays(path, maxHolidays)` with implicit-`#END`-at-EOF behavior (C-divergence — C logs a spurious bug on every boot for the shipped stub that lacks `#END`), `SaveHolidays(path, list)` C-byte-compatible format, `DefaultHolidayAnnounce` constant per C `:130`); `internal/persist/holidays_test.go` (16 tests including shipped-file pin + `_ShippedFileLoadsNoBug` for the implicit-END behavior); `internal/persist/testdata/holidays_{two,empty,malformed,three,no_announce}.dat` (5 fixtures); `internal/act/holidays.go` (349 LOC, 3 commands + `GetHoliday` + `firstToken`/`findHoliday`/`preserveCaseRest`/`maxOrDefault`/`atoi` helpers); `internal/act/holidays_test.go` (32 tests covering all 15 acceptance criteria including the 3 pin-against-C-bug tests + GetHoliday/DoTime integration). Modified: `internal/world/world.go` (+5 LOC, `Holidays []*types.HolidayData` slice alongside Planes); `internal/act/info2.go` (+8 LOC, `DoTime` appends `"&wIt's a holiday today:&W <name>&D\n\r"` when `GetHoliday(TimeInfo.Month, TimeInfo.Day)` non-nil — mirrors C `src/timezone.c:528` under `#ifdef ENABLE_HOLIDAYS`); `internal/boot/boot.go` (+22 LOC, loader + 3 defaults + 3 command registrations; `holidays` Level 0, `saveholiday`/`setholiday` at `LEVEL_ASCENDANT`=60 matching `db/system/en/commands.dat:3864-3878`); `internal/boot/boot_test.go` (+2 tests: missing-file-non-fatal with defaults asserted, scratch-file-loads-2). **3 latent C bugs fixed, all pinned**: (1) `do_setholiday day`'s `atoi(arg3) <= 1` rejection of valid day 1 → Go uses `< 1` intent, `TestDoSetHoliday_DayAcceptsOne`; (2) same `<= 1` bug on `month` → `TestDoSetHoliday_MonthAcceptsOne`; (3) `do_setholiday create` stores `time_info.month` / `time_info.day` unshifted (0-indexed value in 1-indexed slot) → Go stores `TimeInfo.Month+1` / `TimeInfo.Day+1`, `TestDoSetHoliday_CreateAddsToList` pins the +1 translation. **C-divergent design decisions** (all documented inline): (a) loader treats EOF at section-header position as implicit `#END` (shipped file lacks `#END` — C logs spurious bug every boot; Go stays quiet, pinned by `TestLoadHolidays_ShippedFileLoadsNoBug`); (b) `announce <str>` takes the full remainder after `<name> announce` instead of C's single-word `arg3` (the field is prose — default is a 13-word sentence); (c) name-preservation via `firstToken` + `preserveCaseRest` so `setholiday Halloween create` stores `"Halloween"` not lowercased (util.OneArgument lowercases tokens — C's `one_argument` does not); (d) month-list fallback uses a clean `for i, name := range MonthNames` loop, not C's broken `while (month_name[x] != '\0' ...)` which compares `char*` pointer against null char. **Go port fills a C gap**: `GetHoliday(month, day)` is a one-liner lookup against `WorldRef.Holidays`; `DoTime` suffix mirrors C `timezone.c:528` unconditionally (C guards behind `ENABLE_HOLIDAYS`). **Season-tick auto-broadcast DEFERRED** — C at `src/timezone.c:617-631` uses `echo_to_all(AT_IMMORT, day->announce, ECHOTAR_ALL)` on hour-0 of a holiday day; Go-side `weatherUpdate` at `internal/game/update.go:135-154` has no `echo_to_all`/`season_update` equivalent; tracked as TODO follow-up with C citation. **11 mutation gates verified via `Edit` round-trips only** (project banned-command list respected): (1) swap `MonthNames[0]`↔`[1]` → 3 tests fail; (2) drop `m >= len(MonthNames)` OOB guard → `TestMonthName_OutOfRange` panics; (3) drop `Announce` field from struct → compile fail; (4) drop Announce default-seed in loader End branch → `TestLoadHolidays_MissingAnnounceDefaults` fails; (5) swap Month/Day KVP dispatch → `TestLoadHolidays_TwoHolidaysRoundTrip` fails; (6) drop maxHolidays ceiling check → `TestLoadHolidays_MaxHolidaysEnforced` fails; (7) change saver terminator `#END\n`→`#END\r\n` → `TestSaveHolidays_ProducesExpectedBytes` + `EmptyListWritesOnlyTerminator` both fail; (8) drop unknown-section bug log → `TestLoadHolidays_UnknownSectionEmitsSpecificBug` fails (captureBugMessages was added alongside to distinguish from the Garbage-key bug that also fires); (9) swap `+1` to `+0` in create's TimeInfo translation → `TestDoSetHoliday_CreateAddsToList` fails; (10) swap `< 1` back to `<= 1` → `TestDoSetHoliday_DayAcceptsOne` fails; (11) drop duplicate-name check → `TestDoSetHoliday_CreateRejectsDuplicateName` fails; plus drop MaxHoliday check → over-max test fails; plus drop `+1` in GetHoliday translation → both GetHoliday and DoTime-suffix tests fail; plus drop MaxHoliday default at boot → boot defaults test fails; plus unregister saveholiday → boot command-level test fails. **15 total acceptance criteria satisfied** — A1 (17-entry MonthNames + helper), A2 (HolidayData struct 1-indexed), A3 (LoadHolidays shipped-file round-trip), A4 (SaveHolidays round-trip + empty-list terminator + error propagation), A5 (DoHolidays Level 0 via pager), A6 (DoSaveHoliday LEVEL_ASCENDANT), A7 (DoSetHoliday full CRUD), A8 (World.Holidays + boot loader + HolidayFilePath), A9 (MaxHoliday default 32), A10 (build/vet/test-count=3 clean), A11 (`Edit`-only mutation-verify), A12 (shipped-file pin), A13 (3 C-bug pin tests), A14 (GetHoliday + 3 tests), A15 (DoTime suffix + 2 tests). 55 new tests total across 4 packages. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean on all changed/new files, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat.

- **Phase 6 OLC redit landed — interactive `CON_REDIT` room editor** (`plan-phase6-olc-redit.md`). First of three OLC-substate plans. Ports C `src/oredit.c` (1036 LOC) as a menu-driven alternative to the flat `redit <sub> <args>` path from Phase 4b; both paths coexist. 13 task groups, 18 acceptance criteria all satisfied. **Nanny-dispatch pattern established** for `oedit`/`medit` to inherit: `OlcData` struct lives on `DescriptorData` (session-lifetime match for C `d->olc`); `internal/game/loop.go:252-267` processInput dispatch switch gains `case types.CON_REDIT: reditParse(d, line)` which short-circuits nanny for menu input (mirrors C `src/smaug.c:1641-1652`); `DoRedit(ch, "")` no-arg entry allocates Olc + sets Connected + invokes `ReditDispMenuFunc` seam. New files: `internal/types/olc.go` (56 LOC — OlcData with Mode/Vnum/Change/Target/Spare/TempNum fields + 24 `REDIT_*` mode constants `iota+100` so no collision with CON_* iota space); `internal/types/olc_test.go` (4 tests: nil-default, field allocation, mode uniqueness, CON_*-vs-REDIT_* separation); `internal/game/redit_menu.go` (357 LOC — ReditDispMenu + 7 sub-menu renderers + `r_flags` table verbatim from `src/build.c:92-105` + sectorName/sectorKeywords + directionName + exitFlagLabels + getRoomFlagBit helper for word-list flag toggle); `internal/game/redit_parse.go` (448 LOC — reditParse top-level dispatcher + per-mode handlers for all 24 REDIT_* modes + cleanupOlc + olcLog via `util.LogStringPlus` tagged LOG_BUILD + `worldRoomLookup` seam via `SetWorldRef(w)` + uRange/parseIntDefault helpers); `internal/game/redit_parse_test.go` (34 tests); `internal/testclient/redit_test.go` (3 E2E tests). Modified: `internal/types/descriptor.go` (+5 LOC `Olc *OlcData` field), `internal/game/loop.go` (+6 LOC CON_REDIT arm), `internal/act/olc.go` (+20 LOC: extend `DoRedit` no-arg to open menu; add `ReditDispMenuFunc` seam declaration — flat subcommands untouched), `internal/boot/boot.go` (+5 LOC wire `act.ReditDispMenuFunc = game.ReditDispMenu` + `game.SetWorldRef(w)`), `internal/act/olc_test.go` (replace obsolete `TestDoRedit_NoArg` expecting "Redit what?" with two new tests pinning §A5/§A6 behavior). **C-bug fixed (plan §Open Q4)**: `REDIT_EXIT_VNUM` branch at `src/oredit.c:860-875` sets `pexit->vnum = number` but does NOT update `pexit->to_room`, leaving the exit silently pointing at the stale prior target. Go port updates both `pexit.Vnum = n` and `pexit.ToRoom = worldRoomLookup(n)`; pinned by `TestReditParse_ExitVnum_SetsBothFields`. Matches Tranche C Q5 precedent for silently-destructive C bugs. **EditorSave callback semantics verified** (readiness-vet G5 note): the `/s` handler at `internal/game/editor.go:163-172` transitions Connected→CON_PLAYING BEFORE invoking the stashed callback. The main-menu `2` branch and extradesc-`2` branch both re-set Connected=CON_REDIT AFTER StopEditing — the closure does NOT override a live transition, it re-establishes state for the next line. Mirrors C `do_redit_reset` at `src/oredit.c:493-535`. Pinned by `TestReditParse_DescEditorCallback_ReturnsToConRedit`. **Main-menu Q emits "Exiting editor."** not "Room saved." (readiness-vet cosmetic correction): C `cleanup_olc` discards the session; the save-confirm flow is scope-cut (plan §Scope Cuts). **9 mutation gates verified via `Edit` round-trips only** (project banned-command list respected — no `git checkout`/`git restore`/`git stash`/`git reset --hard`/`--amend` across this work): (1) drop `d.Desc.Connected = CON_REDIT` in DoRedit no-arg path → `TestDoRedit_NoArgEntersMenu` fails; (2) swap `CON_PLAYING` → `CON_REDIT` in cleanupOlc → `TestReditParse_Quit_CleansUpAndReturnsToPlaying` + `TestCleanupOlc_ResetsDescriptorAndCharacter` both fail; (3) flip tunnel URANGE upper `1000` → `100` → `TestReditParse_Tunnel_Clamps` fails on 9999 case; (4) replace `Toggle(bit)` with `Set(bit)` in flag-number branch → `TestReditParse_Flags_NumberToggle` fails on second toggle (can't un-set); (5) drop `n == types.SECT_DUNNO` guard in sector validator → `TestReditParse_Sector_RejectsDunno` fails (SECT_DUNNO no longer rejected); (6) drop `pexit.ToRoom = target` (the C-bug-reproducing mutation) in REDIT_EXIT_VNUM → `TestReditParse_ExitVnum_SetsBothFields` fails; (7) disable extradesc empty-junk branch (`if Keyword == "" || Description == ""`) → `TestReditParse_Extradesc_AddKeyQuitJunksIfEmpty` fails; (8) swap `case CON_REDIT:` → `case CON_OEDIT:` in loop.go processInput → `TestTestclient_ReditMenuEntryAndQuit` E2E fails (nanny emits "Unexpected state. Disconnecting."); (9) drop `c.Desc.Connected = CON_REDIT` in the main-menu-`2` EditorSave closure → `TestReditParse_DescEditorCallback_ReturnsToConRedit` fails. **Scope cuts preserved** (plan §Scope Cuts): REDIT_CONFIRM_SAVESTRING flow (constant reserved for future — C already skips it via commented block at `oredit.c:580-586`); `redit_setup_new` (partial Phase 4b `DoRdig` coverage); double-edit guard (world-wide descriptor scan deferred until oedit/medit generalize the lookup); `OLC_CHANGE` dirty-bit (field allocated, no consumer); persisted build-log (olcLog writes via stdlib logger only); fine-grained `can_rmodify` (current `GetTrust() >= LEVEL_IMMORTAL` check retained). **ANSI screen-clear sequence omitted** per plan §Open Q1 — testclient harness cannot interpret cursor-position codes (`"50\x1B[;H\x1B[2J"`); cosmetic-only in C. 43 new tests total (4 types + 34 game + 2 act + 3 testclient). `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean on all changed/new files, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. Commit `f678e8a`.

- **Phase 6 Arena PvP landed — `challenge`/`accept`/`decline`/`withdraw` + arena-victory + 5-tick challenge timeout + AKills/ADeaths persistence** (`plan-phase6-arena.md`). 7 task groups (G1 schema, G2-G4 commands, G5 charUpdate poll, G6 ArenaVictoryCheck, G7 boot wire + E2E), all 15 acceptance criteria satisfied. First true two-player PvP feature in the Go port — exercises `QuickLoginTwo` end-to-end. New files: `internal/act/arena.go` (~310 LOC, 4 commands + state + pickArenaRoom), `internal/act/arena_test.go` (37 unit tests — one per gate in the plan's gate tables), `internal/combat/arena_victory.go` (~130 LOC, `ArenaVictoryCheck` + `stripArenaDebuffs`), `internal/combat/arena_victory_test.go` (11 tests incl. `Damage` integration via the real death-path hook), `internal/testclient/arena_test.go` (3 E2E scenarios: challenge/decline, challenge/withdraw, challenge/accept/teleport). Modified: `internal/types/constants.go` (add `ROOM_VNUM_ARENA_MIN=10366`/`MAX=10382` per `src/mud.h:2283-2284`), `internal/types/enums.go` (add `TIMER_CHALLENGE=8`, the next free iota slot after `TIMER_NUISANCE=7`), `internal/types/constants_test.go` (tag-uniqueness + vnum pin-tests), `internal/persist/player.go` (read/write `AKills`/`ADeaths`; emitter gated on non-zero keeps pfiles clean for non-arena players), `internal/persist/player_test.go` (round-trip + omit-when-zero tests), `internal/handler/timer.go` (new `HasTimer` 3-line wrapper over `GetTimer`; plan §G5 identified this gap in the readiness-vet notes), `internal/combat/skillcheck.go` (extend GSN resolve block with `gsnPoison`/`gsnBlindness`/`gsnSleep`/`gsnCurse` for the arena-debuff-strip pass), `internal/combat/combat.go` (insert `ArenaVictoryCheck(ch, victim)` call BEFORE normal corpse/XP processing at the `POS_DEAD` branch — short-circuits via `rVICT_DIED` return so no corpse spawns in an arena death), `internal/game/update.go` (per-char tick poll firing the timeout when `ACT_CHALLENGER && !HasTimer(TIMER_CHALLENGE) && !ROOM_ARENA`), `internal/game/update_test.go` (4 tests: timeout fires, timer-active-suppresses, in-arena-suppresses, NPC-skipped), `internal/boot/boot.go` (register 4 commands at `POS_RESTING`/Level 0 and wire `combat.ArenaIsBusyFunc = act.SetArenaIsBusy` + `combat.DoLookFunc = act.DoLook`), `internal/boot/boot_test.go` (`TestBoot_RegistersArenaCommands` + `TestBoot_ArenaRoomsFlaggedInShipped` that regex-scans the shipped `newacad.are` to detect OLC-roundtrip drift on the area-data edit). Area-data edit committed: `db/area/newacad.are` 17 rooms in range 10366-10382 get bit 26 added to their RoomFlags (`3145736 → 70254600`, `3145740 → 70254604`). **Open-Q resolutions applied** (all from plan): Q1 `do_decline` C bug preserved verbatim — the decliner's flag is cleared (wrong target, but stays within C fidelity); challenger's flag lingers until 5-tick timeout fires OR victory. Q2 per-player timer via existing `handler.AddTimer` infrastructure (not C's broken single global `challenge_tme`). Q3 `pickArenaRoom` filters by `ROOM_ARENA` flag then randoms within valid set (Option b) — prevents hitting non-arena vnums in the range. Q4 Level 0 / POS_RESTING on all four commands matches C (no explicit command-level trust; the level-5 victim check lives inside `DoChallenge`). Q5 altar-to-temple fallback in `ArenaVictoryCheck` if `ROOM_VNUM_ALTAR` unloaded. Q6 resolved by the area-file edit. Q7 `StopFighting` lives inside the helper (matches C). **C bugs preserved verbatim**: `do_decline` wrong-target flag clear (plan §G4); `do_accept`'s double `do_look(ch)` after second teleport — accepter sees room on second call, victim gets no auto-look (plan §G3). **6 mutation gates verified** via `Edit`-tool round-trips only (banned-command list respected): drop `ch.Act.Set(ACT_CHALLENGER)` → `TestDoChallenge_Success` fails on flag check; weaken level gate `<=5`→`<5` → `TestDoChallenge_VictimLowLevel` fails (victim slips past); drop charUpdate `ch.Act.Remove(ACT_CHALLENGER)` → `TestCharUpdate_ArenaChallengeTimeout` fails; swap `ROOM_VNUM_ALTAR`→`ROOM_VNUM_TEMPLE` in loser teleport → `TestArenaVictory_TeleportAndHeal` fails on loser-room assertion; drop `ch.PCData.AKills++` → `TestArenaVictory_AKillsADeathsIncremented` fails on winner count; drop `ArenaVictoryCheck` call from `Damage` → `TestArenaVictory_NoCorpseSpawned` fails on teleport-out assertion. **~55 new tests total**. Structured self-review substituted for external adversary pass per documented tooling caveat. `go build ./...` clean, `go vet ./...` clean, `gofmt -l .` clean on arena files, `go test -count=3 ./...` green across all 15 packages. Commit `0cc1d97`.

- **Phase 6 Planes landed — `plist` / `pstat` / `pset` + `planes.dat` loader/saver + `CheckPlanes` orphan-assignment** (`plan-phase6-planes.md`). 3 task groups (G1 persist, G2 act, G3 boot integration), 13 acceptance criteria all satisfied. New files `internal/persist/planes.go` (170 LOC) + `internal/persist/planes_test.go` (19 tests, including BugSink capture for the `Name`-empty and unknown-key branches) + four testdata fixtures (`planes_empty.dat`, `planes_two.dat`, `planes_cformat.dat`, `planes_malformed.dat`); `internal/act/planes.go` (188 LOC) + `internal/act/planes_test.go` (23 tests). Boot wiring at `internal/boot/boot.go` adds loader invocation + `act.PlanesFilePath` set + `persist.CheckPlanes(w, nil)` seed, plus three `reg.Register` calls (plist Level 0, pstat LEVEL_IMMORTAL, pset LEVEL_GREATER). Four boot-level tests added to `boot_test.go`. Three testclient E2E tests in new `internal/testclient/planes_test.go` exercise mortal `plist`, admin `pset create`/`plist`/`pset delete`, and `pset save` persistence to disk with cleanup restoring pre-existing state. **A1 empty-slice-vs-nil distinction implemented**: missing file returns `(nil, nil)`; stub `#END\n` file returns `(non-nil empty slice, nil err)`; these two branches are distinguishable by callers. `CheckPlanes` keys off `len(w.Planes) == 0` so it seeds "Prime Material" correctly in both branches. **Tilde-tolerant Name reading** accepts both C-format `Name      Astral\n` (a latent C format mismatch — C `save_planes` writes no tilde but C `read_plane` uses `fread_string` which expects one, per `src/planes.c:181` vs `:220`) and SMAUG-convention `Name      Astral~\n`; saver always emits tilde-terminated. **Splice-delete algorithm** (post-audit correction) uses standard `append(s[:i], s[i+1:]...)` — works uniformly for first/middle/last index, unlike the original plan's broken "swap-to-end + remove-last" which would have silently corrupted mid-index deletions in 3+ element slices. `TestDoPset_DeleteMiddlePlane` + `TestDoPset_DeleteFirstPlane` + `TestDoPset_DeleteLastPlaneSymmetric` + `TestDoPset_DeleteLastPlaneRebuildsPrime` pin all four slice-position cases. **SmashTilde on rename** is deliberate C-divergence (C's `do_pset name` does NOT smash tildes in the new name — a latent bug class since tildes break SMAUG file I/O); Go port applies `util.SmashTilde` before storing, matching `DoTitle`/`DoBio`/`DoDescription` precedent. **10 mutation gates verified** via `Edit`-tool round-trips only (no `git checkout`/`git restore`/`git stash` per project banned-command list): drop `TrimSuffix "~"` → `TestLoadPlanes_TildeTerminatedRoundTrip` fails; skip empty-Name rejection → `TestLoadPlanes_BlockWithNoNameDropped` fails; drop Prime Material seed in `CheckPlanes` → `TestCheckPlanes_EmptySliceAppendsPrimeMaterial` fails; drop `deleted==room.Plane` branch → `TestCheckPlanes_DeletedMatchReassigned` fails; drop `planeLookup` pass-2 → `TestDoPstat_PrefixMatchPrintsName` fails; swap `HasPrefix` args → `TestDoPset_CreatePrefixMatch` fails; remove `SmashTilde` → `TestDoPset_RenameSmashTilde` fails; drop `CheckPlanes` in `DoPset delete` → 3 tests fail (demonstrates the plan-noted stronger mutation from the orchestrator readiness vet — the planned weaker "move `CheckPlanes` after splice" mutation indeed does not flip any test, but the outright drop does); drop 3 `reg.Register` calls → `TestBoot_RegistersPlaneCommands` fails; drop boot `CheckPlanes(w, nil)` → `TestBoot_AssignsEveryRoomAPlane` + `TestBoot_LoadsPlanesAndSeedsPrimeMaterial` fail. **Deliberate C-divergences**: `do_pstat` emits `\n\r` (C emits `\n`) to match Go line-ending convention; `SavePlanes` emits tilde-terminated names (C emits space-terminated — a latent C format mismatch fixed in the port); `DoPset name` applies `SmashTilde`. **Preserved from C**: `planeLookup` two-pass exact-then-prefix semantics; `OneArgument` lowercases `arg1` so stored names come in lowercase (matches C `STRALLOC(arg)` after `one_argument` which also lowercases); rename duplicate-check uses `planeLookup` on the new name which resolves to the current plane itself as a prefix match when renaming to a same/prefix name — same false-positive as C. `World.Planes []*types.PlaneData` slice added to `world.go` in the Clans/Councils/Deities block. `RoomIndexData.Plane *PlaneData` and `PlaneData { Name string }` already existed at `types/room.go:26,111-114` (verified, zero type additions). Files modified: `internal/world/world.go` (+5 LOC, Planes field), `internal/boot/boot.go` (+17 LOC, loader + register calls), `internal/boot/boot_test.go` (+~90 LOC, 4 boot tests), `CLAUDE.md` (index row flipped to LANDED). Files added: `internal/persist/planes.go`, `internal/persist/planes_test.go`, `internal/persist/testdata/planes_empty.dat`, `planes_two.dat`, `planes_cformat.dat`, `planes_malformed.dat`, `internal/act/planes.go`, `internal/act/planes_test.go`, `internal/testclient/planes_test.go`. `go build ./...` clean, `go vet ./...` clean, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for adversary pass per CLAUDE.md tooling caveat. Commit `d895e5b`.

## 2026-04-18

- **Phase 6 Skills landed — `bloodlet` / `pounce` / `broach`** (`plan-phase6-skills.md`). Three independent player commands ported from `src/skills.c:2607-2695` (do_pounce), `:3517-3565` (do_bloodlet), `:3960-4011` (do_broach) plus three `boot.go` registrations. All 16 acceptance criteria satisfied. Two latent C bugs fixed per plan Option-A: (1) `do_bloodlet`'s operator-precedence gate `IS_NPC(ch) || !IS_VAMPIRE(ch) || !IS_DEMON(ch)` would require BOTH predicates simultaneously (functionally dead for typical single-identity vampire or demon characters); Go `isBloodRace` helper ports the reconstructed intent `!IS_NPC && (vampire || demon)`; (2) `do_broach`'s fail-predicate `!CLOSED || !LOCKED || PICKPROOF || can_use_skill` is tautological on most doors AND inverts the skill-check sense; Go ports the reconstructed intent `success := CLOSED && LOCKED && !PICKPROOF && can_use_skill`. Both bugs cited in-code with C line numbers and plan Open Question references. `gsnPounce` combat-side (`MultiHit` single-hit short-circuit at `internal/combat/combat.go:247`) already shipped; only command entry added. `OBJ_VNUM_BLOODLET` prototype already present at `db/area/en/limbo.are2:806-811`. Bloodlet `ch.Wait` uses literal `PULSE_VIOLENCE` (not `skills.dat` Beats=12) per C `:3535` — intentional C divergence preserved verbatim. Deliberate omissions vs. C (all match existing Go skill convention): `is_safe`/`check_attacker`/`check_illegal_pk` PK-legality helpers unported (no skill uses them); `adjust_favor` deity subsystem unported; `check_room_for_traps` trap dispatcher unported; `set_char_color` per-descriptor colour unported — broach uses inline `&g` token; bloodlet uses `util.Act(types.AT_BLOOD, ...)` and renders uncolored until `util.atColorCode` gains an AT_BLOOD entry. 41 tests added across `skills3_test.go` (14 DoPounce), `skills_test.go` (11 DoBroach), `skills4_test.go` (16 DoBloodlet + isBloodRace). 5 mutation-verify round-trips confirmed via `Edit` tool only (weapon-type set membership, broach success predicate, bloodthirst decrement, Wait constant, reverse-exit lock removal) — every mutation failed its pinned test. Files modified: `internal/act/skills.go` (+~70 LOC DoBroach + doc comment), `internal/act/skills3.go` (+~85 LOC DoPounce), `internal/act/skills4.go` (+~90 LOC DoBloodlet + isBloodRace), `internal/boot/boot.go` (+3 registrations), `internal/act/skills_test.go` (+11 tests), `internal/act/skills3_test.go` (+14 tests), `internal/act/skills4_test.go` (+16 tests). `go build ./...`, `go vet ./...`, `go test -count=3 ./...` all green across all 15 packages. Commit `1d929e8`.

- **Phase 6 Starmap landed** (`plan-phase6-starmap.md`). `look sky` ported from `src/starmap.c:87-226` + dispatch branch added to `DoLook` at `internal/act/info.go:58-66`. 3 task groups (G1 tables + helpers, G2 LookSky + dispatch, G3 integration pins), 13 acceptance criteria. New files `internal/act/starmap.go` (250 LOC) and `internal/act/starmap_test.go` (628 LOC, 37 tests). File-private constants `starmapWidth=72`/`starmapHeight=8`/`starmapNumDays=35`/`starmapNumMonths=17`/`starmapWeathUnit=10` — no cross-package surface since no other subsystem reads `weath_unit`; if the immortal `weather` command ports later, promote to `world.World`. Constellation table (`starMap` 8x72), sun glyph (`sunMap` 3x5), moon glyph (`moonMap` 3x5) copied byte-for-byte from `src/starmap.c:59-85`. Pure renderer `renderStarmap(hour, day, month, rawPrecip int) []string` returns header + 3 plot rows (daytime, hour in 6..18) or header + 8 plot rows (night) or header + cloud line (bucket > 1). `LookSky(ch *types.CharData)` appends `"\n\r"` per line on emit; guards on `ch==nil || WorldRef==nil` (silent) and `ch.InRoom.Area.Weather==nil` (cloudy fallback sentinel). Indoor check uses flag-OR-sector canonical pattern (`ROOM_INDOORS` flag OR `SectorType==SECT_INSIDE`) matching `internal/magic/spell_unique.go:281-282` and `internal/mudprog/ifcheck.go:640-643`; intentional Go-convention divergence from C's flag-only `IS_OUTSIDE`. Case-insensitive via `strings.EqualFold`. **A11 fidelity deviation documented**: plan said eclipse at `(hour=12, day=0)` emits `&W@` (moon) + `&Y|` (sun); C semantics per `starmap.c:132-136` show moonphase=0 masks every moon cell as `" "`, producing a 5x3 black-disk cutout over the sun cells — so NEITHER `&W@` nor `&Y|` appears at the day-0 eclipse. Go port preserves C fidelity: `TestDoLook_SkyEclipseBlocksSunAt_Day0Noon` pins the inverse, and `TestDoLook_SkyContainsSunAtNoon` pins `&Y|` at `day=5` (non-eclipse noon). 7 `Edit`-round-trip mutations all caught: NUM_DAYS 35→36 breaks position math; weath_unit 10→1 breaks precip bucket; single byte in `starMap[1]` (`O:`→`O.`) breaks transcription guard; drop `SECT_INSIDE` clause lets outdoor-render leak into sector-INSIDE rooms; remove `if moonphase > 4 { -= 8 }` clamp gives phase=5 instead of -3; `&Y`→`&R` in sun glyph breaks noon-sun pins; `\n\r`→`\n` line terminator breaks line-count integration tests. Rejected plan alternatives captured in `plan-phase6-starmap.md` §Go Design B-G. No boot-registration change (look is already registered). Scope cuts: no `weath_unit` runtime tunability, no gettext, no pager, no `help sky`, no testclient golden-file, no per-player override. `go build ./...` clean, `go vet ./...` clean, `go test -count=3 ./...` green across all 15 packages. Commit `cd6f1b3`.

- **Phase 6 Extra Channels landed** (`plan-phase6-channels-extra.md`). Six new communication channels — `music`, `racetalk`, `wartalk`, `counciltalk`, `guildtalk`, `newbiechat` — routed through a new shared `talkChannel` helper in `internal/act/channels.go`. 6 task groups (G0 helper + G1-G5 wrappers + G6 docs), 18 acceptance criteria all satisfied. Helper owns empty-arg rejection, `PLR_SILENCE` sender gate, deaf-sender block (with wartalk/yell exception — C's always-true `||` bug corrected to intent-form `&&`), `ROOM_SILENCE`, self-echo, and broadcast walk. Per-channel wrappers pass a `recipientFilter` closure: pkill-only for wartalk; `Race` equality for racetalk; pointer-identity on `PCData.Council` for counciltalk; `PCData.Clan` pointer + `ClanType == CLAN_GUILD` for guildtalk; immortal-or-"Newbie Council" member (case-insensitive) for newbiechat. Tier 9 channels (`DoImmtalk`/`DoGtell`/`DoClantalk`/`DoAuction` stub) kept open-coded per plan — non-retrofit scope. 7 mutation gates verified via `Edit` round-trips (wartalk/yell exception preservation, self-echo-no-rebroadcast, filter-closure application, pkill gate, council pointer eq, guild ClanType check, newbie case-insensitivity). Deliberate divergences: no `PLR_WIZINVIS` preamble / `ROOM_LOGSPEECH` append / `is_ignoring` filter / `AFLAG_SILENCE` / per-AT_ color / `NOT_AUTHED` gate / wait-state — all match existing Go channel convention; uniform follow-ups queued. Verified-absent in stock data: `db/councils/council.lst` is empty, so `newbiechat` is immortal-only by default — matches C exactly for the same configuration. Files modified: `internal/act/channels.go` (+~160 LOC: helper + 6 wrappers + `isNewbieChannelMember`), `internal/act/channels_test.go` (+~700 LOC, +37 tests), `internal/boot/boot.go` (+6 registrations). `go test -count=3 ./...` green across all 15 packages. Commit `5ff02b6`.

- **Phase 6 planning Wave D — 3 plans authored + 3 audits landed. All 14 Phase-6 plans are now externally audited** (Wave A/B/C/D cumulative). Six `manager` lineages dispatched across two phases: (1) `phase6-olc-redit` authored `smaug-go/doc/plan-phase6-olc-redit.md` — Interactive Room Editor `CON_REDIT`. 13 task groups, 18 criteria, 6 open questions. First of three OLC-substate plans; proves nanny-dispatch pattern for `oedit`/`medit` to inherit. `OlcData` struct on `DescriptorData` (session-lifetime matches C). Extends `DoRedit` to open menu on no-arg; preserves flat subcommands. Identifies C bug at `src/oredit.c:860-875` — `REDIT_EXIT_VNUM` assigns `pexit->vnum` but not `pexit->to_room`, leaving exits asymmetric; plan recommends fix-in-Go (matches Tranche C Q5 precedent for silently-destructive bugs). (2) `phase6-polymorph` authored `smaug-go/doc/plan-phase6-polymorph.md` — 7 task groups (G0-G6), 20 criteria, 7 open questions. Schema complete (CharData.Morph + CharMorph 32 fields + MorphData 65 fields at `character.go:66,270-372`). Combat hook design: **direct stat mutation in new `internal/handler/polymorph.go`**, NOT routed through `AffectModify` — C's `do_morph` at `polymorph.c:1473-1579` mutates `ch->armor`/`mod_*`/saves/hitroll/damroll/hit/move/mana directly and stashes deltas on `ch->morph` for reversal; 25+ synthetic affects wouldn't match C's `mod_*` semantics. Handler-package placement avoids act↔mudprog import cycles. Mudprog dispatch entries already at `mudprog/driver.go:260-265`; handler bodies at `commands.go:545-554` are stubs. (3) `phase6-archery` authored `smaug-go/doc/plan-phase6-archery.md` — 6 task groups, 15 criteria. Adds `WEAR_LODGE_*` slots + `ITEM_LODGE_*` wear-flag bits + `ITEM_LODGED` extra-flag + `PROJ_*` kinds; ports `DoDraw`/`DoFire`/`DoDislodge` + full `ProjectileHit`/`RangedAttack`/`ScanForVictim`/`RangedGotTarget` stack. G5 in dispatch prompt (melee-loop ranged hook) resolved as **omit** on C-fidelity grounds — `grep WEAR_MISSILE_WIELD src/fight.c` returns zero hits. G7 quiver auto-reload cut as new gameplay. Catalogued 3 C bugs for verbatim-port: `do_draw` omits `WEAR_DUAL_WIELD` in hand-count (`:142-149`); `do_dislodge` arm damage `3*v[1]..2*v[2]` asymmetry (`:234`); `do_fire` dead `victim==ch` check on never-assigned pointer (`:1255,1272`). (4) `audit-olc-redit` — verdict **PASS with minor CONCERNS**. Verified 11/11 C citations (including `REDIT_EXIT_VNUM` bug at `oredit.c:860-875` confirmed real) and 8/8 Go citations. Applied 2 surgical in-plan edits: G5 EditorSave closure wording ("override default CON_PLAYING" → "re-set Connected AFTER StopEditingFunc" — `/s` handler at `game/editor.go:163-172` transitions to CON_PLAYING BEFORE invoking closure, so closure re-sets rather than overrides); G12 added testclient-prompt-tracking note (`ReditDispMenu` has no trailing `> ` sentinel, scenarios must match menu-text substrings). Design decisions sustained: `OlcData` on `DescriptorData`, REDIT_EXIT_VNUM fix-in-Go, 13-group decomposition. (5) `audit-polymorph` — verdict **PASS with CONCERNS (self-review)**. Schema-complete claim **confirmed** via 5-field spot-check (deity, obj[3], dayfrom, hitroll dice-string, no_cast — all present, consistent types). Applied 5 corrections: G4 morphset LOC 700→889 (G4a/G4b split now mandatory per `polymorph.c:72-960`); G6 Vnum-before-Name pfile ordering documented per `fread_morph_data` at `polymorph.c:2405-2590`; G0.2 DiceParse exists as package-private `magic.parseDiceExpr` at `spell_smaug.go:455` — plan now promotes rather than reimplements; G0.3 and G2.3 UpdateAris call dropped with rationale per `player_affect_test.go:11-22` Go-invariant-documented no-update_aris; G2.4 resource-leak ordering of `do_morph_char` at `polymorph.c:1262-1384`. **Two plan claims falsified**: `AT_MORPH` already at `enums.go:208`, `COND_BLOODTHIRST` already at `enums.go:884` — G0.1/G0.4 become verification-only. (6) `audit-archery` — verdict **CONCERNS**. All 3 flagged C bugs verified verbatim at `archery.c:142-149, 234, 1255/1272`. OMIT-combat-hook verdict **confirmed** (`grep WEAR_MISSILE_WIELD src/fight.c` → 0 hits). Applied 3 in-place edits. **Schema-gap spot-check shrank G1 scope**: `ROOM_NOMISSILE` already at `enums.go:722` (Q6 resolved); `PLR_NICE` at `:975` (Q7 resolved); `findDoor` at `move.go:12` (plan was unsure); `CharData.NumFighting` at `character.go:78` (Q8 partially resolved). **New gap not in plan**: `ITEM_WEAR_MAX=21` at `constants.go:588` must bump to 24 after WEAR_LODGE_* additions — G1 now includes this. **New finding not in plan**: `mob_fire:1352` uses `bow->value[4] != arrow->value[5]` vs `do_fire:1295` uses `[5]!=[4]` — value-index asymmetry (open item for mob_fire follow-up). **Lodged-arrow `remove` vs `dislodge` gap**: C doesn't set `ITEM_NOREMOVE` on lodged arrows — players can `remove` to bypass dislodge damage; plan silent, flagged as follow-up. **Tooling caveat applying to all 6 Wave-D lineages**: `Agent` tool unavailable in `manager` subagent harness across this entire Wave; structured self-review substituted throughout. Files modified: `smaug-go/doc/plan-phase6-olc-redit.md`, `plan-phase6-polymorph.md`, `plan-phase6-archery.md`.

- **Phase 6 planning Wave C audit pass — 3 new-plan audits landed (stances-olc re-dispatched after stall, completed on re-run).** All 11 Phase 6 plans now audited (arena, hotboot, starmap, holidays, marriage, planes, channels-extra, skills, stances-olc, auction, clan-officer).

- **Phase 6 planning Wave C new-plan audits (auction + clan-officer + stances-olc).** (3) `audit-stances-olc` — verdict **CONCERNS**. First dispatch stalled at stream watchdog before writing drafts (600s no-progress); re-dispatched with time-boxed prompt instructing early-write-then-refine, completed successfully. Verified 13 C citations against `src/stances.c:51-1022` (do_stance state machine, update_stances bit toggle, can_use_stance 4-gate flow, load_stances/fread_stance/fwrite_stance, do_ststat newline-every-4 bug at `:687`, do_stset 4 preserved bugs at `:823-826` class no-op, `:858-861` dual inverted, `:918-921` race no-op, `:276-279` class/race IS_SET-mask-AND-index). Verified 9 Go-state claims against HEAD (`character.go:128-130`, `constants.go:196-217`, `stance_index.go:29-50`, `persist/stances.go:15-164`, `skills4.go:180-199`, `skills4_test.go:110-116`, `boot.go:521` stance-already-registered, `boot.go:290-299` loader wiring, `world.go:76` DataDir). **Correctness-critical finding (#1)**: plan's §D3 `UpdateStances` impl mixes `int` (CharData fields at `character.go:128-130`) with `uint32` (RIS_* constants at `constants.go:196-217`) without noting the cast requirement at fixture/test call sites — compile-error risk at G2 without inline note. **Citation corrections**: (a) §Q6 inline `RisflagNames` draft table stops at "plus7" but actual `constants.go` defines 22 bits terminating in `magic` (bit 20) and `paralysis` (bit 21) — 22 not 20 names; (b) §Q9 `stance` registration verified at `boot.go:521` (POS_DEAD / Level 0) — closes Q9 pre-dispatch. **Q8 resolved independently**: `World.DataDir` confirmed at `world.go:76`, Option (a) preferred over package-level var. Four preserved C-bug claims all verified verbatim; one intentional fix (`do_ststat` newline operator-precedence) verified sound. Existing `TestDoStance_Set` test at `skills4_test.go:110-116` will break under new CanUseStance-gated DoStance; plan's §G2 Risk Analysis correctly flags with working mitigation (swap `"dragon"` → `"viper"`). 4 surgical in-plan edits applied. Files modified: `smaug-go/doc/plan-phase6-stances-olc.md`.

- **Phase 6 planning Wave C new-plan audits (auction + clan-officer).** (1) `audit-auction` — verdict **CONCERNS**. 4 factual errors corrected in 4 in-place edits: (a) anomaly #1 `can_carry_w` check is REACHABLE not dead code — during 27s auction window the seller is a free agent who can acquire new weight; (b) anomaly #2 C has an ACTIVE `do_quit` auction gate at `act_comm.c:2883-2890` (plan claimed reliance on `mud.h:3614` comments only) — Go port now uses C's verbatim message `"Wait until you have bought/sold the item on auction."`; (c) anomaly #14 `ms_find_obj` at `handler.c:2941` is a generalized drunk/mental-state check called from `get/drop/put/quaff/recite/eat/drink/auction/pick` — not a mud-school quest hook as plan claimed (port-as-no-op still correct, rationale fixed); (d) §D7 + Scope Cuts — C ships a full `do_noauction` immortal command at `act_wiz.c:11120-11174` with list/toggle/persist, load-only Go port is honestly tracked as deferred follow-up instead of "C has nothing to port". All function-span, pulse-cadence, struct-layout, tax-formula, gate-list, and state-machine-branch citations verified PASS. **Pre-existing bug flagged**: `pulseSave` field at `internal/game/loop.go:68` is never initialized in `NewGameLoop` (`loop.go:83-94`) — autosave fires on the first pulse after boot. Warning carried forward: when adding `pulseAuction`, it MUST appear in the struct literal. Two-layer disconnect defense retained as pure C-parity (C's do_quit gate + Go-originated `closeDescriptor` defensive clear for TCP-level disconnects). (2) `audit-clan-officer` — verdict **CONCERNS → PASS after 3 surgical edits**. **Scope-correction claim independently verified**: `grep -rn "do_promote\|do_demote" src/` returns zero hits — `phase6-roadmap.md:205` is wrong; plan correctly scopes to `induct`/`outcast`/`bestow` + optional G5 `setrank`. C citations for all four command bodies (clans.c:177-255 save_clan 79 LOC, clans.c:928-1097 do_induct 170 LOC, clans.c:1168-1340 do_outcast 173 LOC, act_wiz.c:7079-7135 do_bestow 57 LOC) verified; branch matrix at clans.c:981-1050 and `add_loginmsg` at clans.c:1323 verified. Go citations verified (`DoInduct`/`DoOutcast`/`DoBestow`/`SaveClan` absent; `ClanData` struct has all C-written fields so no schema change needed; `clanDir` at `boot.go:322` is a local var). 3 surgical edits: (a) added missing `Abbrev` key to the loader-missing-keys list (original plan omitted it); (b) flagged pre-existing legacy-PKills index bug (Go loader at `subsystems.go:66-69` stores into `clan.PKills[0]`, C `fread_clan:435` stores into `[6]` — pre-existing divergence folded into G0 loader rework); (c) neutralized one-sided Q1 recommendation (both "include G5 setrank" and "defer to `plan-phase6-setclan.md`" are defensible). (3) `audit-stances-olc` — first dispatch stalled at stream watchdog before writing any drafts; re-dispatched with time-boxed prompt. **Tooling caveat**: both completed audits fell back to structured self-review; `Agent` tool remains unavailable in manager harness across all Wave-C audit lineages. Files modified: `smaug-go/doc/plan-phase6-auction.md`, `plan-phase6-clan-officer.md`.

- **Phase 6 planning Wave C — 2 audits + 3 new plans.** Five `manager` lineages dispatched in parallel. (1) `audit-starmap` — verdict **PASS with 3 fidelity corrections** applied in-plan: (a) **critical** row-6 constellation-table transcription error in the first fenced block corrected from `"        C*    ..."` to `"        c.    ..."` to match `src/starmap.c:66` — exactly the class of bug the plan's own Risk #1 flagged as most dangerous; (b) precip-bounds citation corrected from `src/update.c:3377-3378` to the actual URANGE site at `:3446-3452`; (c) `DoWeather` indoor-check mis-described as flag-OR-sector — corrected to flag-only at `info.go:264`; canonical flag-OR-sector pattern lives at `internal/magic/spell_unique.go:281-282` and `internal/mudprog/ifcheck.go:640-643`. All 8 constellation rows re-verified byte-for-byte (72 chars each); sun/moon/precip hand math re-verified. (2) `audit-holidays` — verdict **CONCERNS**. Three material C-reality corrections: (a) `get_holiday` has 2 live callers at `src/timezone.c:528,622` under `#ifdef ENABLE_HOLIDAYS` (plan claimed zero — season-tick-announce machinery may need porting after all); (b) no `do_load holiday` runtime-reload command exists in C (plan's claim was fabricated); (c) new Open Q6 recommends landing `GetHoliday` + `DoTime` holiday-today suffix in this plan. Five in-plan edits. (3) `phase6-stances-olc` — authored `smaug-go/doc/plan-phase6-stances-olc.md`. 5 task groups, 20 criteria, 10 open questions. Extends `StanceInfo` from 3 combat fields to the full 19-field C-cognate shape (Resist/Immune/Suscept/Dodge/Parry/Dual/MaxWeight/Wait/Prereq/Class/Race/SpecialMove/SpecialPercent/Self/Others); upgrades loader to store non-combat keys; adds `SaveStances` writer; ports `can_use_stance` + `update_stances`; rewrites `DoStance` state machine (today's stub is name-only and has a hard-coded 7-stance slice missing `viper`/`crane`/`crab`/`normal` — Tranche B Tier-6 test `TestDoStance_Set` will need update when `CanUseStance` lands); adds `DoSTstat`/`DoSTset` OLC. **Preserves 4 C bugs with pin-tests**: `stset class <N>` sets `found=TRUE` with no assignment (silent no-op), same for `stset race`, `stset dual 1` stores 0 (inverted), `can_use_stance` uses `IS_SET(class_restrictions, ch->class)` where class is a bare index not a bit. **Fixes 1 C cosmetic bug**: `do_ststat` newline-every-4 used `!index_num % 4` (operator precedence) — newline never fires, corrected to `index_num % 4 == 0`. Depends on Tranche B G1 (shipped). (4) `phase6-auction` — authored `smaug-go/doc/plan-phase6-auction.md`. 10 task groups, 23 criteria, 8 open questions. Completes the Tier-9 `BroadcastAuction` stub by porting C `act_obj.c:3775-4240` (non-GSC variant) + `update.c:3183-3326` (auction_update). Adds `pulseAuction` slot to game loop; ports `parsebet`/`advatoi`/`num_punct` helpers; extends `AuctionData` with History ring + HistTimer; two-layer disconnect defense (`DoQuit` refuses when `ch == Auction.Seller || Auction.Buyer && Auction.Item != nil`, `closeDescriptor` defensively cancels with refund + item drop). **17 C bugs/anomalies catalogued**: 1-gold tax math drift (`(int)(bet*0.1) + (int)(bet*0.9) != bet` for some values); dead-code carry-weight check (seller always has room); `ms_find_obj` mud-school hook absent in Go; seller/buyer-disconnect relies on non-existent quit-gate. (5) `phase6-clan-officer` — authored `smaug-go/doc/plan-phase6-clan-officer.md`. **Scope-corrected finding**: `do_promote`/`do_demote` do NOT exist in shipped SMAUG — `phase6-roadmap.md:205` was wrong. Rank management is via `do_setclan number1/number2/leader`, captured as optional G5 (Q1). Actual C LOC 478 vs roadmap 300. SaveClan bundled as G0 (Option A rationale: ~60 Go LOC pure text serialization, symmetric with existing loader, every officer command ends with `save_clan(clan)`, separate plan adds 4h orchestrator overhead for zero design benefit). Existing Go `loadClan` is incomplete — misses LeadRank/OneRank/TwoRank/ClanObjFour/ClanObjFive/MemLimit/PKillRangeNew/PDeathRangeNew; round-trip fidelity requires G0 loader extension. Existing `DoClanJoin` at `internal/act/clan.go:101` is a zero-gate stub, NOT a port of `do_induct`. 5 task groups (+1 conditional), 14 criteria, 4 open questions. **Tooling caveat applying to all 5 Wave-C lineages**: `Agent` tool unavailable inside the `manager` subagent harness despite `manager.md` spec listing it; structured self-review substituted. External adversary passes for the 3 new plans queued. Files modified: `smaug-go/doc/plan-phase6-starmap.md`, `plan-phase6-holidays.md`. Files added: `smaug-go/doc/plan-phase6-stances-olc.md`, `plan-phase6-auction.md`, `plan-phase6-clan-officer.md`.

- **Phase 6 planning Wave B audit pass — 4 external adversary audits landed.** Four `manager` lineages dispatched sequentially after Wave B plan authoring: (1) `audit-hotboot` re-run (prior lineage left empty drafts) — verdict **PASS with notes**. 28 C citations verified against `src/hotboot.c:73-854` (save_mobile L73, save_world L143, load_mobile L206, read_obj_file L433, load_obj_files L524, load_world L552, do_hotboot L599, hotboot_recover L744, ROOM_VNUM_LIMBO fallback L350, ROOM_VNUM_TEMPLE fallback L830, CON_COPYOVER_RECOVER sentinel L813). 19 Go-state claims verified (HomeVnum `character.go:209`, RoomVnum `object.go:90`, PCData.Hotboot `pcdata.go:117`, Boot `boot.go:78`, `act.WorldRef` `boot.go:81`, ClearTimerRegistry `boot.go:152`, GameLoop.Run/Cancel/SavePlayer, LoadPlayerWithWorld, SavePlayer(io.Writer), DescriptorData, server.go readLoop, World.go:12, EditorSave `character.go:60`, main.go:18-70). Five in-place edits: header audit-status block, area-file bug list expansion (gods.are L115/L119/L123 = mis-dispatched section headers; newacad.are has ~70 further errors), `(*net.TCPConn).File()` Windows caveat per Go stdlib ("fd not usable on other processes") — strengthens Open Q1 to double-lock, `syscall.Dup2` portability note (linux/arm64 needs `syscall.Dup3(old, new, 0)`), PoC log-port-drift explanation. Seven unresolved questions added (pfile-schema migration, partial-line readLoop buffering, sentinel-mob HomeVnum at `handler.go:14` as pre-G1, mid-login `ch->desc` vs `d` C bug at hotboot.c:675, linux/arm64 Dup3, G6 integration-test harness budget, LOC re-verification). (2) `audit-planes` — verdict **CONCERNS**. 25 C citations + 21 Go citations verified. Caught one algorithmic bug: `DoPset delete`'s swap-to-end-then-remove-last pattern silently corrupts mid-index deletions in 3+ element slices (removes the wrong element) — replaced with standard `append(slice[:idx], slice[idx+1:]...)` splice, verified by hand on first/middle/last index scenarios. Caught one factual misstatement: `one_argument` DOES support `'` and `"` quote syntax in both C (`src/interp.c:1143-1144`) and Go (`internal/util/strings.go:22-25`), so multi-word plane names ARE addressable as `pset "Prime Material" delete`. Q2 rewritten. G2 test suite expanded to 3 delete-index scenarios. Seven minor line-offset corrections. 10 in-place edits total. (3) `audit-channels-extra` — verdict **CONCERNS**. 5 occurrences of nonexistent `util.TranslateFor` across §Problem / §Go Current State / §G0 pseudocode / §G0 tests / §Seams corrected to `translateFor` (unexported, package-private at `internal/act/comm.go:13`). Verified `db/councils/council.lst` is empty in stock data — so `newbiechat` is immortal-only by default (matches C semantics). Wartalk `||`-bug at `src/act_comm.c:507` confirmed structurally always-true; Go `&&` repair correct. All 6 commands.dat entries verified (counciltalk L599, guildtalk L1249, music L2287, newbiechat L2649, racetalk L3587, wartalk L4557). 10 in-place edits. No execution blocker. (4) `audit-skills` — verdict **PASS with minor clarifications**. All C citations verified against `src/skills.c:2607-2695` (do_pounce), `:3517-3565` (do_bloodlet), `:3960-4011` (do_broach), plus `src/mud.h:2048` and `:4029-4034`. All Go-state claims verified (gsnPounce `combat/skillcheck.go:110`, MultiHit short-circuit `combat/combat.go:247`, `OBJ_VNUM_BLOODLET=26` `constants.go:404`, blood-pool prototype `limbo.are2:806-811`, DoBackstab at Level 0 `boot.go:488`). All `skills.dat` metadata (Minpos/Rounds/Minlevel/Mana/Code/Dammsg) matches plan table. Two in-place clarifications: status upgraded; Q1 "functionally unreachable" refined — `IS_VAMPIRE`/`IS_DEMON` macros expand to race-OR-class disjunctions, so the gate IS reachable for dual-identity characters (race=RACE_VAMPIRE + class=CLASS_DEMON) while being functionally dead for typical single-identity characters. Option A (fix-the-bug) recommendation stands. One follow-up flagged: Risk 1 (self-damage) depends on unverified `combat.Damage(w, ch, ch, ...)` self-target safety; pre-flight test or citation needed before G3 execution. **Tooling caveat applying to all 4 audits**: `Agent` tool was unavailable inside the `manager` subagent harness despite manager.md listing it — structured self-review substituted for true independent adversary dispatch. TODO.md already tracks this as an environment-level investigation item. Files modified: `smaug-go/doc/plan-phase6-hotboot.md`, `plan-phase6-planes.md`, `plan-phase6-channels-extra.md`, `plan-phase6-skills.md`.

- **Phase 6 planning Wave B in-flight — 3 new plan docs + 1 completed audit.** Five manager lineages dispatched in parallel: (1) `phase6-planes` landed `smaug-go/doc/plan-phase6-planes.md` (760 LOC) — `plist`/`pstat`/`pset` + `CheckPlanes` orphan-assign + `system/planes.dat` round-trip. Zero schema additions — `RoomIndexData.Plane *PlaneData` at `internal/types/room.go:26` and `PlaneData` struct at `:111-114` already exist. Loader uses tilde-tolerant parse to accept both C-saver space-terminated format and SMAUG-convention tilde-terminated format; saver emits tilde-terminated. (2) `phase6-channels-extra` landed `smaug-go/doc/plan-phase6-channels-extra.md` (559 LOC) — 6 channels (`music`/`newbiechat`/`racetalk`/`wartalk`/`counciltalk`/`guildtalk`). Factor `talkChannel` helper with 6 thin wrappers; channel-toggle table already covers all 6 bits post `plan-do-channels.md`. Flags C-bug `||` in wartalk deaf-exception condition; Go port uses the intent form `&&`. (3) `phase6-skills` landed `smaug-go/doc/plan-phase6-skills.md` (417 LOC) — `bloodlet` / `pounce` / `broach`. Flags 2 latent C bugs: bloodlet's operator-precedence bug makes the command unreachable in C (Go port fixes); broach's missing predicate branch (Go port reconstructs from branch structure). Three parallel-safe work units. (4) `audit-marriage` re-audited `plan-phase6-marriage.md` — verdict **CONCERNS**. Uncovered **vnum-100/101 collision**: `db/area/newgate.are` registers non-ring objects (candelabra + magical spring) at vnums 100/101 in the shipped tree, so `GetObjIndex(100)` / `GetObjIndex(101)` return non-nil non-ring prototypes in production. The plan's original nil-guard strategy for `DoRings` does not fire; Q2 resolution space broadened to 7 options (new vnums, remove newgate entries, runtime shape validation, boot-time synthetic registration, etc.). Plan's criterion count typo fixed (10→12); header status upgraded. Upstream doc drift flagged: `CLAUDE.md`/`TODO.md`/`phase6-roadmap.md` reference "7 groups G0/G0b/G7" but plan is self-consistent at G1-G5. (5) `audit-hotboot` — lineage dispatched; plan-phase6-hotboot.md edited in-place with factual cleanups (net.Conn File() docs, CON_COPYOVER_RECOVER negative-state sentinel, ROOM_VNUM_LIMBO-vs-TEMPLE distinction at mob vs player recovery fallbacks, HomeVnum/RoomVnum schema-present correction). Drafts folder empty; audit verdict unrecorded — scheduling re-audit. Remaining adversary passes queued for hotboot / planes / channels-extra / skills — lineages dispatched via `manager` subagent type (which has `Agent` tool) rather than `general-purpose` (which does not). Files added: `smaug-go/doc/plan-phase6-planes.md`, `plan-phase6-channels-extra.md`, `plan-phase6-skills.md`. Files modified: `smaug-go/doc/plan-phase6-marriage.md` (audit-marriage in-place edits), `smaug-go/doc/plan-phase6-hotboot.md` (audit-hotboot in-place edits).

- **Phase 6 planning Wave A landed — 2 audits + 4 new plan docs.** Six manager lineages ran in parallel: (1) `audit-roadmap` applied 21 factual corrections to `smaug-go/doc/phase6-roadmap.md`. Key finding: five of twelve items in the roadmap's "Gaps in the Existing Go Port" list were wrong — `MorphData`+`CharData.Morph`, `CharData.X/Y/Map/Sector`, `RoomIndexData.Plane`+`PlaneData`, `PCData.Spouse`, and `ITEM_PROJECTILE`/`ITEM_QUIVER` are all already defined; only `WEAR_LODGE_*` (3 slots) truly missing. Starmap no-`sky`-branch-in-DoLook corrected. Auction `update.c:2886` → `2927/3183` citation corrected. Cascading Scope/Dependency/Risk-tier text in Polymorph/Archery/Planes/Marriage/Overland/Dragonflight sections aligned with schema-present reality. (2) `audit-arena` applied 6 Edits to `plan-phase6-arena.md`: `TIMER_CHALLENGE` next iota slot is 8 (not 7 — TIMER_NUISANCE occupies 7); `handler.AddTimer` signature is `(ch, tType, count int, doFun string, value int)` — `doFun` precedes `value`, plan's `(ch, TIMER_CHALLENGE, 5, 0, "")` would not compile; `ROOM_VNUM_ALTAR = 21194` already exists at `types/constants.go:433` — do not re-declare; `ArenaVictoryCheck` inline snippet switched from direct `act.SetArenaIsBusy` to `ArenaIsBusyFunc(false)` hook to match the Seams narrative. Arena-flag absence on vnums 10366-10382 (bit 26 not set in room flags 3145736/3145740) confirmed — plan Open Question 6 remains a live prereq. (3) `phase6-hotboot-design` landed `smaug-go/doc/plan-phase6-hotboot.md` (513 LOC) as an explicit **design-exploration plan, not executable**. Two Go designs evaluated against C baseline (`src/hotboot.c:73 save_mobile`, `:143 save_world`, `:206 load_mobile`, `:599 do_hotboot`, `:744 hotboot_recover`). Design A: `syscall.Exec` + FD inheritance via `(*net.TCPListener).File()` + `(*net.TCPConn).File()` + `net.FileListener` + `net.FileConn` + `syscall.Dup2`. Design B: save-state + graceful-restart + reconnect-cookie + `os.StartProcess` child. **Both PoCs built, compiled, ran, and PASSED** in `/tmp/hotboot-poc-a/` and `/tmp/hotboot-poc-b/` (throwaway scratch, not committed). Design A preserves TCP 5-tuple across exec (~65ms total pause); Design B forces client reconnect (~85ms + reconnect). Plan **recommends Design A** with 9 open questions (Windows support = Design A Linux/macOS-only is the critical one). Executable follow-up plan is a full file rewrite once adversary-reviewed and Q1 resolves. (4) `phase6-starmap` landed `smaug-go/doc/plan-phase6-starmap.md` (589 LOC) — `look sky` port; 3 task groups, 13 acceptance criteria. 8x72 constellation table + 3x5 sun/moon glyphs + moon-phase masking preserved verbatim from `src/starmap.c:59-79`. Self-review caught and fixed a row-7-col-17 constellation-token misattribution; upgraded transcription-guard test from first-10-char prefix to full-row equality. (5) `phase6-holidays` landed `smaug-go/doc/plan-phase6-holidays.md` (690 LOC) — 4 task groups, 13 acceptance criteria. Ports `month_name[]` + `MonthName()` helper + `HolidayData` struct + persistence at `db/system/holidays.dat` + `DoHolidays`/`DoSaveHoliday`/`DoSetHoliday`. Deliberate C-bug fixes: day/month validation `<=1` rejecting 1 corrected to `<1`; `create` store `TimeInfo.Month+1` to align with 1-indexed file format. Shipped `holidays.dat` has no `#END` terminator — EOF-as-implicit-END treated as success without bug log. (6) `phase6-marriage` landed `smaug-go/doc/plan-phase6-marriage.md` (669 LOC) — 7 task groups, 12 acceptance criteria. Found that `PCData.Spouse` is an **orphan field** (never read/written) while `CharData.Spouse` is canonical (LoadPlayer reads `ch.Spouse` at persist/player.go:285); `SavePlayer` has no matching writer — load/save asymmetry. Plan's G0 removes the orphan; G0b adds the missing SavePlayer writer. Ring vnums `OBJ_VNUM_DIAMOND_RING=100` and `OBJ_VNUM_WEDDING_BAND=101` (roadmap's prior `STEEL_RING` naming was wrong — corrected in roadmap in this same commit). 5 open questions; Q1 (level-10 gate) resolved in-plan as omit (dead code); Q2 (ring prototypes missing from shipped area data) escalated to human; Q3-Q5 documented with plan recommendations. **All six lineages lacked the `Agent` tool** in the manager subagent harness, so independent-adversary dispatch was not possible. Each performed structured self-review documented in its completion report; orchestrator recommends an external adversary pass before any executable plan dispatches. Files added: `smaug-go/doc/plan-phase6-hotboot.md`, `plan-phase6-starmap.md`, `plan-phase6-holidays.md`, `plan-phase6-marriage.md`. Files modified: `smaug-go/doc/phase6-roadmap.md` (21 corrections from audit + STEEL_RING→WEDDING_BAND fix), `smaug-go/doc/plan-phase6-arena.md` (6 fact corrections).

- **Phase 6 planning begun.** Phase 5 marked Complete (17 tiers + Tranches A/B/C, last-landed 2026-04-18 at `26db5f9`). Two new docs under `smaug-go/doc/`: `phase6-roadmap.md` (385 LOC) inventories every Phase 6 candidate system across 4 categories (infrastructure, game systems, OLC, content) with C-LOC-verified estimates, per-system dependencies, proposed plan-doc filenames, and a recommended 6-wave execution order (Wave 0 hotboot-design → Wave 1 small self-contained systems → Wave 2 hotboot-execute + auction + stances-OLC → Wave 3 interactive-OLC → Wave 4 big game systems → Wave 5 dragonflight → Wave 6 content polish). Risk matrix + cross-plan dependency table + 7 open questions for human input. `plan-phase6-arena.md` (687 LOC) is the first executable Phase 6 plan: Arena PvP with 7 task groups and 15 acceptance criteria covering `DoChallenge` / `DoAccept` / `DoDecline` / `DoWithdraw` + `TIMER_CHALLENGE` timeout tick + combat-victory branch (`combat.ArenaVictoryCheck` teleports loser→altar, winner→temple, heals, strips poison/blindness/sleep/curse, increments Akills/Adeaths); E2E testclient scenario + area-data prerequisite callout (`db/area/newacad.are` vnums 10366-10382 may need `ROOM_ARENA` flag). `smaug-go/doc/phases.md` Phase 5 status updated to Complete; Phase 6 section added with pointer to the roadmap. Planning-manager lineage had no `Agent` tool so self-review was performed in lieu of adversary dispatch — caught and fixed 8 concrete errors in the Arena plan (util.Act signature, method names, constant values, level gates). External adversary pass on both planning docs is queued in TODO.md before the first Phase 6 execution lineage dispatches. Files affected: `smaug-go/doc/phase6-roadmap.md` (new), `smaug-go/doc/plan-phase6-arena.md` (new), `smaug-go/doc/phases.md`.

- **Tranche C — landed (plan-tranche-c.md G1-G8, 10 acceptance criteria).** Eight task groups covering `util.Act` per-call color parameter + 33-site migration, `update_aris` audit correction, `PLR_BLANK` blank-line emission, XP-on-skill-gain + adept-cap message. All 10 acceptance criteria met; `go build ./...` clean, `go vet ./...` clean, `go test -count=3 ./...` green across 15 packages. **G1 — AT_* constants pinning test.** `internal/types/enums.go:159-217` already declared the full `at_color_types` enum (AT_COLORBASE=1024 through AT_TOPCOLOR=1080, AT_MAXCOLOR=56) matching C `src/mud.h:1107-1166`. New `internal/types/constants_test.go` pins every value. No `AT_CLANTALK` constant added (doesn't exist in C — the original plan-table draft mislabel was corrected in the pre-execution adversary pass). **G2 — `util.Act` per-call color parameter.** Signature changed from `Act(format, ch, vch, a1, a2, to)` to `Act(aType int, format, ch, vch, a1, a2, to int)`. New `atColorCode` map in `internal/util/act.go` populates 18 commonly-used AT_* constants with `&X` color codes (Go convention — C default_set[] values differ deliberately, see plan). `sendActTo` prepends the code and splices `&D` reset just before the trailing `\n\r`. Unknown AT values emit no prefix (safe default). New `internal/util/act_color_test.go` covers AT_HIT prefix, AT_PLAIN no-prefix, unknown-AT no-panic, AT_HITME dark-red, AT_IMMORT yellow, AT_GTELL magenta. Mutation-verified by stripping `&D` suffix. **G3 — 33 caller-site migrations** across `internal/combat/dammessage.go` (9), `internal/act/cmds2.go` (7), `internal/act/skills3.go` (7), `internal/act/skills4.go` (8), `internal/act/playercfg.go` (2). Damage-message callers use `AT_HIT` for TO_CHAR, `AT_HITME` for TO_VICT, `AT_ACTION` for TO_NOTVICT (matches C `fight.c:4524-4554`, `:4586-4588` two-call pattern). Skill narration uniformly `AT_ACTION`. Stun victim-side "smashes into you" → `AT_HITME`. All existing tests pass unchanged because they match against message fragments tolerant of the color prefix. **G4 — Migration verification.** `grep -rn 'util\.Act(' internal/ --include='*.go' | grep -v '_test.go' | grep -v '// '` returns exactly 33 matches, every one with 7 arguments. Compile-time type checking enforces this invariant going forward. **G5 — `update_aris` audit correction + regression test.** `smaug-go/doc/plan-player-config.md` R5 section amended explaining why Go does not need `update_aris`: C clears `affected_by`/`resistant`/`immune`/`susceptible` on every affect change and saves base-minus-equipment stats (C `src/handler.c:1221-1305`, `src/save.c:213`); Go instead maintains stats incrementally via `handler.AffectModify(ch, aff, fAdd)` (internal/handler/handler.go:386-455). New `internal/persist/player_affect_test.go:TestSaveLoadPlayer_AffectRemovalMaintainsStatInvariant` pins the invariant: base Armor=-10 → apply APPLY_AC buff -20 → expire + AffectRemove → Armor restores to -10 → SavePlayer/LoadPlayer round-trip preserves. Mutation-verified: flipping AffectRemove's `AffectModify(..., false)` to `true` → test red. **G6 — `PLR_BLANK` blank-line emission.** New unexported helper `writePromptWithBlank(d)` in `internal/game/loop.go` checks `d.Character.Act.IsSet(PLR_BLANK)` and writes `"\n\r"` ahead of the formatted prompt. Nil-safe on descriptor and character. Two prompt-emit sites converted: pulse-loop `:258` (post-command) and `enterGame:767` (initial post-login prompt). Matches C `src/smaug.c:1359-1361`. Four tests in `internal/game/loop_plrblank_test.go`. No user-facing toggle shipped (scope cut — `DoBlank` follow-up). **G7 — XP-on-skill-gain + fully-learned message.** `learnFromSuccess` in `internal/act/skills.go:46-83` rewritten into a two-branch form matching C `src/skills.c:1641-1669`. Normal-gain (learned < adept): `20 * skLvl` XP with ×6 mage / ×3 cleric multipliers; message suppressed when `Fighting != nil` or `gsn == gsnHide` or `gsn == gsnSneak`. Adept-cap (learned == adept after UMIN clamp): `1000 * skLvl` XP with ×5 mage / ×2 cleric multipliers, `"&WYou are now an adept of %s!  You gain %d bonus experience!\n\r&D"`. Branches mutually exclusive. `skLvl` falls back to `ch.Level` when `skill.SkillLevel[class] == 0`. Prereq: new package-private `gsnHide`/`gsnSneak` vars (default -1) in `act/skills.go` and new exported `act.ResolveGSNs()` wired from `internal/boot/boot.go:134`. 10 XP tests + 3 ResolveGSNs tests; four mutation verifications all caught. **G8 — Plan wrap-up.** Completion record appended to `smaug-go/doc/plan-tranche-c.md`. Files affected: `internal/types/constants_test.go` (new), `internal/util/act.go`, `internal/util/act_test.go`, `internal/util/act_color_test.go` (new), `internal/combat/dammessage.go`, `internal/act/cmds2.go`, `internal/act/skills3.go`, `internal/act/skills4.go`, `internal/act/playercfg.go`, `internal/act/skills.go`, `internal/act/skills_xp_test.go` (new), `internal/act/skills_gsn_test.go` (new), `internal/boot/boot.go`, `internal/game/loop.go`, `internal/game/loop_plrblank_test.go` (new), `internal/persist/player_affect_test.go` (new), `smaug-go/doc/plan-player-config.md` (R5 amendment), `smaug-go/doc/plan-tranche-c.md` (completion record).

- **Tranche B — landed (plan-tranche-b.md G1-G4, 18 acceptance criteria).** Five task groups shipped in one coordinated lineage: G1 stances loader, G1b DoMset stance-name extension, G2 TIMER_DO_FUN callback registry + expiry dispatch, G3 seven mudprog if-checks, G4 wordlist match + OprogCommandTrigger + RprogCommandTrigger rewrite. All 18 acceptance criteria A1-A18 met; `go vet ./...` clean; `go test -count=3 ./...` green across all 15 packages. Implementation mutation-verified via `Edit` round-trips exclusively per the manager destructive-git ban. **G1 — Stances data loader:** new `internal/persist/stances.go` (~140 LOC + ~180 LOC tests) exports `LoadStancesInto(target *[types.MAX_STANCE]combat.StanceInfo, path string) error` and `GetStanceNumber(name string) int` mirroring C `load_stances` / `fread_stance` / `get_stance_number` at `src/stances.c:311-382` / `:469-593` / `:199-232`. Fail-soft: missing file is a no-op, bad stance names log `util.Bug` and skip, unknown keys drain-and-log, the shipped 4-byte `End\n` stub is a full no-op (defaults preserved). Loader overwrites only combat-relevant fields (NumAttacks, DamDone, DamTaken); non-combat keys (Class, Immune, Resist, Suscept, Dodge, Dual, Parry, Percent, Race, Stance, Special, Other, Self, Wait, Weight) consume their payload types so the scanner advances, but values discarded pending Phase-6 `StanceInfo` extension (plan G1.3). Five fixtures under `internal/persist/testdata/stances_*.dat` + `TestLoadStances_RealShippedStubLoads` reads the real `db/system/stances.dat`. Wired into `boot.bootDB` after skills load. 9 loader tests + 1 `GetStanceNumber` case-matrix test. **G1b — DoMset stance-name branch:** `internal/act/olc_set.go` `DoMset` extended with a stance-name branch in the `default:` case matching C `src/build.c:3499-3537`, ordered after all named fields, before the usage-generator. Uses `persist.GetStanceNumber(field)` with the C-faithful `> 0` guard (STANCE_NONE=0 silent no-op, test-pinned). NPC path writes to both `victim.Stances` (instance) and `victim.IndexData.Stances` (prototype) when IndexData present. PC path gates on `ch.GetTrust() < types.LEVEL_LESSER` (=57) with C-faithful `"You can only modify a mobile's immunities.\n\r"`. Clamp `val < 0 || val > MAX_{PC,MOB}_STANCE=200` with `"Stance value is from 0 to 200\n\r"`. Defensive: non-NPC without PCData returns `"Cannot set stance mastery on that target.\n\r"`. 7 new tests. No player-facing `practice stance <name>` — C has none; mastery is seeded by admin only. **G2 — TIMER_DO_FUN registry + expiry dispatch:** `internal/handler/timer.go` +~90 LOC. New `TimerFunc` type alias; package-level `timerRegistry map[string]TimerFunc` with `RegisterTimerFunc` / `LookupTimerFunc` / `ClearTimerRegistry`. `DecrementTimers` gains expiry-dispatch: when a `TIMER_DO_FUN` timer expires with non-empty `DoFun`, `dispatchExpiredDoFun` saves `ch.Substate`, sets it to `timer.Value` (C `fight.c:421`), invokes the registered callback, restores substate. If `LookupTimerFunc` returns nil, logs `util.Bug` and drops. Callback re-extension (`timer.Count > 0` after return per C `fight.c:425-426`) preserves the timer. Empty `DoFun` drops silently. Self-adversary caught aliasing hazard in `kept := ch.Timers[:0]` — resolved by allocating a fresh `make([]*TimerData, 0, len(ch.Timers))` slice with `TestDecrementTimers_DoFunAppendsNewTypeTimer` regression. Scope cuts documented in function doc-comment: mid-decrement intercept (combat-aborts-skill, `fight.c:386-398`) and interp intercept (`interp.c:713-733`) deferred until first skill command (`do_detrap`, `do_dig`, `do_search`, `do_mend`, `do_reading`, `do_cast`) ports. New `util.BugSink func(string)` test seam in `internal/util/log.go` + `SetBugSink(fn)` enables the unknown-DoFun test to observe the bug log. Registry cleared at top of `boot.Boot` for re-boots. Boot comment enumerates the 6 C skills that will register here when they port. 10 G2 tests. **G3 — Mudprog if-checks (7 newly wired, 2 remain):** `internal/mudprog/ifcheck.go` gets new cases for `timeskilled`, `objtype`, `leverpos`, `pkadrenalized`, `asupressed`, `areamulti`, `multi`. `timeskilled` reads `chk.IndexData.Killed` with nil-safety on PC chkchar (returns false, no panic); `argStr` vnum-fallback walks `WorldRef.GetMobIndex`. `objtype` reads `chkObj.ItemType`. `leverpos` ports the *intent* of C's buggy-OR guard (`src/mud_prog.c:1493-1495`): requires `chkObj.ItemType ∈ {ITEM_SWITCH, ITEM_LEVER, ITEM_PULLCHAIN}` via AND of positive matches, reads `chkObj.Value[0] & TRIG_UP`, compares `wantsUp` (from rval=="up") against `isUp`. `pkadrenalized` / `asupressed` delegate to `handler.GetTimer(chk, TIMER_RECENTFIGHT)` / `TIMER_ASUPRESSED`. `areamulti` / `multi` iterate `WorldRef.Characters` with host-descriptor match; both guard `!chk.IsNPC() && !c.IsNPC() && c.Desc != nil && chk.Desc != nil` per C. NPC chkchar yields count=0 which goes through `compareInt` rather than early-false — pinned by `TestIfCheck_Multi_NPCChkcharReturnsFalse`. Still deferred: `isflagged` / `istagged` (needs `VariableData` + `get_tag` subsystem port). 18 new tests in `internal/mudprog/ifcheck_g3_test.go`. **G4 — Wordlist match + Oprog/Rprog command-trigger ports:** new `internal/mudprog/wordlist.go` (~80 LOC) exports `wordlistMatch(arglist, input string) bool` and `containsAtWordBoundary(text, needle string) bool` porting C's `rprog_wordlist_check` / `oprog_wordlist_check` at `src/mud_prog.c:4109-4178` / `:3813-3880`: lower-case both sides; `"p "` prefix treats remainder as ONE phrase; otherwise split arglist on whitespace; left boundary = start-of-text or preceded by `' '`; right boundary = end-of-text or followed by `' '`, `'\n'`, `'\r'` (tab/punctuation do NOT satisfy — matches C). `RprogCommandTrigger` rewrite: replaces ~40 LOC of buggy first-word-exact-match with single `wordlistMatch(prog.ArgList, line)` call; `strings` import dropped. 3 new Rprog tests including `SubstringWithoutWordBoundaryDoesNotFire` (regression against pre-rewrite impl that couldn't match non-first-word keywords). `OprogCommandTrigger` implemented: iterates `ch.InRoom.Contents` only (C-faithful room-floor per `src/mud_prog.c:3569-3580`), skips objs without IndexData, skips prog entries without `MPROG_CMD` bit, fires first match. 4 new Oprog tests replacing `TestOprogCommandTrigger_Stub` — `CarryingObjDoesNotMatch` pins the A16 correction from the plan's external adversary pass (plan v1 erroneously said "carried too"). 12 wordlist helper tests. Files affected: `internal/persist/stances.go` (new), `internal/persist/stances_test.go` (new), 5 fixture .dat files, `internal/act/olc_set.go` (+45), `internal/act/olc_set_test.go` (+168), `internal/handler/timer.go` (+113), `internal/handler/timer_registry_test.go` (new), `internal/mudprog/ifcheck.go` (+124, -5), `internal/mudprog/ifcheck_g3_test.go` (new), `internal/mudprog/oprog.go` (+25), `internal/mudprog/oprog_test.go` (+62), `internal/mudprog/rprog.go` (-15), `internal/mudprog/rprog_test.go` (+53), `internal/mudprog/wordlist.go` (new), `internal/mudprog/wordlist_test.go` (new), `internal/util/log.go` (+18 BugSink seam), `internal/boot/boot.go` (+22: stances load + ClearTimerRegistry wire). Plan completion record appended to `smaug-go/doc/plan-tranche-b.md`. Executed in autonomous mode by a single sub-manager session (structured self-adversary passes — the session acted as both implementer and reviewer; one aliasing concern caught during G2 self-review produced an extra fix + regression test).

- **Tranche A polish — six items landed.** `bio` / `description` commands + Bio persistence + `[AFK]` `who` marker + `%X` XP-to-next-level prompt token + `DoCircle`/`DoHitall` retcode defense-in-depth + testclient Go-idiom cleanup (`interface{}` → `any`, `bytes.ToLower`/`Index`, scratch reuse) + `channels +all`/`-all` derived from `channelToggleTable.publicAll`. All six pass `go test -count=3 ./...` across 15 packages. (1) `DoBio` / `DoDescription` at `internal/act/playercfg.go:145-213` wire editor-save closures that write `ch.PCData.Bio` and `ch.Description` respectively when `/s` fires; NPC / nil-PCData / PCFLAG_NOBIO / PCFLAG_NODESC guards mirror C `player.c:3366-3464`. `Bio` field added to `SavePlayer` at `internal/persist/player.go:515-520` (closes TODO R1 — load path at `player.go:218` already consumed it). Registered at `POS_DEAD`/level 0 in `internal/boot/boot.go:392-393`. E2E test in `internal/testclient/bio_test.go` drives a full `bio` → editor prompt → text → `/s` → `save` → `quit` → relogin → verify-preserved cycle. Mutation-verified: wrong-target assignment (Bio → Description) fails `TestDoBio_EditorSaveClosureUpdatesBio`; NPC-guard removal fails `TestDoBio_NPCIsNoop`; NOBIO-guard removal fails `TestDoBio_PCFLAG_NOBIO_Blocked`; SavePlayer Bio-skip fails `TestSaveLoadPlayer_BioRoundTrip`. All 4 mutations caught via `Edit` round-trips (no destructive git). NPC branch in DoBio now sends `"Mobs cannot set a bio.\n\r"` matching C `player.c:3416-3418`; DoDescription sends `"Monsters are too dumb to do that!\n\r"` matching C `player.c:3370`. Level-5 gate from C `player.c:3420` intentionally omitted matching the local DoTitle / DoAfk convention of deferring content gates. (2) `DoWho` at `internal/act/info.go` now prepends `"[AFK] "` to PC listings when `rch.Act.IsSet(types.PLR_AFK)`, matching C `act_info.c:3686` format. Tests: `TestDoWho_AfkPlayerShowsAfkMarker` (positive) + `TestDoWho_NonAfkPlayerHasNoAfkMarker` (negative); both mutations (force-off → positive fails; force-on → negative fails) caught. (3) `%X` prompt token at `internal/game/prompt.go:76-82` now computes `expToLevel(ch, level+1) - ch.Exp` with non-negative clamp. New package-level seam `game.PromptExpBase func(*CharData) int` (wired in `internal/boot/boot.go:140-156` from `w.Classes[ch.Class].ExpBase`, or 1000 for NPCs per C `handler.c:107-112`). `expToLevel` helper implements C's `lvl = max(0, level-1); return lvl^3 * get_exp_base(ch)` at `prompt.go:30-37`. Three new prompt tests (`_NoSeam` fallback, `_UsesSeam` positive, `_NeverNegative` over-exp clamp) + one boot-wire assertion in `boot_test.go:161-163`. Mutation-verified both directions (stub → positive test fails; boot-wire removal → boot test fails). (4) `combat.AttackerDied(retcode int) bool` and `combat.VictimDied(retcode int) bool` helpers exported at `internal/combat/combat.go:21-38`; `DoHitall` at `internal/act/skills3.go` now breaks on `combat.AttackerDied(ret) || ch.Position <= POS_DEAD` (matches C `skills.c:5334` `retcode == rCHAR_DIED || rBOTH_DIED || char_died(ch)`); `DoCircle`'s second swing now guards on `!VictimDied(ret) && !AttackerDied(ret) && victim.Position > POS_DEAD && ch.Position > POS_DEAD`. Production path for attacker-death is dormant in the current Go codebase (no fireshield / ice_shield / acid_shield yet — verified via grep) so a live driven-through-OneHit/Damage test is not possible; helper predicates themselves are mutation-verified in `combat_test.go` (`rVICT_DIED` flip → `TestAttackerDied` fails). Scope-cut rationale captured in inline comment above the helper. (5) Testclient Go-idiom polish at `internal/testclient/client.go`: `fatalf` signature `...interface{}` → `...any`; `readUntilErr` search on `bytes.ToLower`/`bytes.Index` on `[]byte` (old path had `strings.ToLower(string(c.buf))` allocating per iteration); `scratch []byte` field on `Client` lazy-init once on first `fillOnce` (was `make([]byte, 4096)` per call). Two new tests: `TestClient_ScratchBufferReused` asserts `&c.scratch[0]` identity across calls; `TestClient_ReadUntil_CaseInsensitive_AfterBytesRefactor` verifies case-insensitive semantics preserved. Mutation-verified: reinstate per-call `make` → `ScratchBufferReused` fails. `strings` import dropped from client.go (replaced by bytes). (6) `publicAll` slice at `internal/act/channels.go:247` replaced by a `publicAll bool` field on each `channelToggleTable` entry; the `+all`/`-all` handler now iterates the table filtered on `entry.publicAll`. AVTALK remains separately gated on `ch.Level >= LEVEL_IMMORTAL` (C `act_info.c:5497`) because the gate is a player property, not a channel property. Two new tests: `TestDoChannels_PublicAllDerivedFromToggleTable` (flip `tells` to publicAll=true and verify it enrolls) + `TestDoChannels_PrivateChannelsNotToggledByAll` (whisper stays untouched). Mutation-verified both directions (CHAT flag → public-set test fails; drop filter → all three sister tests fail). Files affected: `internal/act/playercfg.go` (+~70 LOC DoBio/DoDescription), `internal/act/playercfg_test.go` (+~200 LOC bio/description tests), `internal/act/info.go` (+~8 LOC AFK marker), `internal/act/info_test.go` (+~40 LOC AFK tests), `internal/act/skills3.go` (+~14 LOC retcode guards in DoCircle/DoHitall), `internal/act/skills3_test.go` (+~50 LOC DoCircle + doc comment on dormant DoHitall path), `internal/act/channels.go` (+~25 LOC publicAll refactor), `internal/act/channels_test.go` (+~60 LOC derivation tests), `internal/boot/boot.go` (+bio/description registers + PromptExpBase wire), `internal/boot/boot_test.go` (+3 LOC PromptExpBase nil-reset + assertion), `internal/combat/combat.go` (+~18 LOC AttackerDied/VictimDied helpers), `internal/combat/combat_test.go` (+~35 LOC helper tests), `internal/game/prompt.go` (+~20 LOC expToLevel + %X fix), `internal/game/prompt_test.go` (+~60 LOC seam tests + EachToken cleanup), `internal/persist/player.go` (+~6 LOC Bio write), `internal/persist/player_test.go` (+~60 LOC Bio round-trip + empty-not-emitted), `internal/testclient/client.go` (interface{}→any, bytes.ToLower/Index, scratch field), `internal/testclient/client_test.go` (+~45 LOC scratch/case-insensitive tests), `internal/testclient/bio_test.go` (new, ~110 LOC E2E test). `go test -count=3 ./...` green across all 15 packages. Executed in autonomous mode by a single sub-manager session (no worker/adversary agent tool available — the session acted as both implementer and reviewer, with every change mutation-verified via `Edit` round-trips per the manager's destructive-git ban). Parent manager will commit alongside Tranches B and C.

- **Editor `/s` save — landed (plan-editor-save.md G1-G6).** Three parallel wave-2 plans landed in one session using the manager-decomposed worker-plus-adversary workflow (this one, `plan-do-gag.md`, and `plan-rollD20-seam.md`). Prior to this fix, `/s` in the editor only sent `"Done.\n\r"` and returned — the descriptor stayed in `CON_EDITING` permanently and every subsequent line the player typed was routed back to `EditBuffer` (`loop.go:261-264`). Builders who used `redit desc` were silently wedged; the edited text was dropped. The fix implements the plan's Option C (call-site assignment, zero signature churn): `internal/types/character.go:60` adds `EditorSave func(*CharData)` field adjacent to `Editor *EditorData`. Callers in `internal/act/olc.go` set `ch.EditorSave = func(ch *types.CharData) { ... }` **before** invoking `StartEditingFunc(ch, text)`. The closure captures the target pointer (the `*RoomIndexData` for `redit desc`, the `*ExtraDescrData` for `redit ed`), calls `CopyBufferFunc(ch)` to extract text, writes to the captured target, calls `StopEditingFunc(ch)`, and emits `ch.Send("\n\r")` for post-transition prompt spacing (plan § G4 caveat — the pulse loop's `CON_PLAYING` branch at `loop.go:252-259` has already run for this tick, so the next prompt doesn't fire until the next input). The `/s` handler at `internal/game/editor.go:159-173` now: (1) transitions `ch.Desc.Connected` to `CON_PLAYING` first, (2) one-shot-clears `ch.EditorSave` before invoking it (prevents double-invoke on re-enter — nested edit inside the save closure works correctly), (3) calls the saved callback, (4) does NOT call `StopEditing` directly — the callback owns cleanup (matches C `build.c:7004-7010`). `StopEditing` at `editor.go:83-94` additionally nils `ch.EditorSave` defensively (plan Open Q #1) so a stray `StopEditing` without a preceding `/s` doesn't leak the closure. The `act` package cannot import `game` (circular dep — same reason `StartEditingFunc` exists), so two new package-level seams `CopyBufferFunc` / `StopEditingFunc` were added at `internal/act/olc.go:26-27` and wired at `internal/boot/boot.go:136-137` alongside the pre-existing `StartEditingFunc` wire. `boot_test.go` updated in two places: lines 98-99 nil-reset the new seams (so the test doesn't depend on prior test wiring) and lines 148-153 assert the wires are non-nil after boot — the second set of assertions was a first-pass adversary finding (gap: the nil-reset existed but the post-boot check didn't, so a regression dropping the wire from `boot.go` would go undetected and manifest as silent-no-op at `/s`). 9 new tests: in `internal/game/editor_test.go` the empty `TestEditBuffer_SaveCommand` stub was rewritten into a real assertion + 5 siblings (`TestEditBuffer_SaveInvokesCallbackAndTransitions`, `TestEditBuffer_SaveCallsCallback_ClearsState`, `TestEditBuffer_SaveNoCallback_StillTransitions` — no-panic with nil callback, `TestEditBuffer_AbortDoesNotInvokeCallback` — `/a` path, `TestEditBuffer_SaveDoubleFireUsesOneShotClear` — one-shot property); in `internal/act/olc_test.go` three tests (`TestDoRedit_SetsEditorSave`, `TestDoRedit_EdSetsEditorSave`, `TestDoRedit_DescSaveRoundTrip` — the round-trip manually simulates `/s` because `act → game` import is prohibited; test comment documents the structural reason); in `internal/types/character_test.go` `TestCharData_EditorSaveField` pins field existence + nilability. 7 mutations exercised via `Edit` round-trips only — never destructive git per the manager-wide ban established after the timer-subsystem workers inadvertently ran `git checkout -- combat.go` and destroyed uncommitted work. Mutations: field removal (`TestCharData_EditorSaveField` fails to compile — green post-revert), transition delete (`SaveInvokesCallbackAndTransitions` + `SaveNoCallback_StillTransitions` fail), invocation delete (4 tests fail), one-shot clear removal (`SaveDoubleFireUsesOneShotClear` fails with 2 invocations vs 1), wrong-target assign in callback (`DescSaveRoundTrip` fails on description assertion), `CopyBufferFunc` wire removal (`TestBoot_WiresCallbacks` fails with `"act.CopyBufferFunc not wired"`), `StopEditingFunc` wire removal (same for `StopEditingFunc`). All caught; all reverted cleanly. Option C invariants held: `func StartEditing(ch *CharData, text string)` signature unchanged, `var StartEditingFunc func(ch *CharData, text string)` signature unchanged, `internal/boot/boot.go:133` `act.StartEditingFunc = game.StartEditing` unchanged, `internal/boot/boot_test.go:422` `TestMainGoHasNoCallbackWires` regex on `act\.StartEditingFunc\s*=` still matches boot.go unmodified. Adversary review PASS with one CONCERNS finding on the boot_test.go gap (described above); a narrow follow-up worker added the post-boot assertions and mutation-verified them via temporary wire removal; manager verified via direct Read of the resulting 6-line addition and full-suite re-run. Unblocks `DoBio` / `DoDescription` (plan-player-config.md R6, now shippable via the same `EditorSave` callback pattern) and Phase-6 interactive OLC substates (`CON_OEDITING` / `CON_MEDITING` — the testclient's `WithPrompt` seam was staged for this). Files affected: `smaug-go/internal/types/character.go` (+1), `smaug-go/internal/types/character_test.go` (+~14), `smaug-go/internal/game/editor.go` (+~14 `/s` + 1 nil in `StopEditing`), `smaug-go/internal/game/editor_test.go` (+~200), `smaug-go/internal/act/olc.go` (+2 seams + 2 closures at `:63-73` and `:138-146`), `smaug-go/internal/act/olc_test.go` (+~80), `smaug-go/internal/boot/boot.go` (+2 wires at :136-137), `smaug-go/internal/boot/boot_test.go` (+2 nil-resets at :98-99 + 2 assertions at :148-153). `go test -count=3 ./...` green across all 15 packages. See completion record at the bottom of `smaug-go/doc/plan-editor-save.md`.

- **`DoGag` player command — landed (plan-do-gag.md G1-G4).** `PCFLAG_GAG` was already defined (`types/constants.go:652`), honored (`combat/dammessage.go:342-349` via Tier 7 dammessage gaps), and persisted (`persist/player.go:243` load, `:520` save) — but no player-facing command set it, so the flag was effectively test-only. This landing ships the command. `DoGag` at `internal/act/playercfg.go:68-83` mirrors `DoAfk` at `:40-53` exactly: `IsNPC` gate, load-bearing nil-`PCData` guard (`IsNPC()` checks `ch.Act.ACT_IS_NPC` only — it does NOT check `PCData`, so a malformed PC with nil `PCData` would pass `IsNPC()` and crash on `.Flags`; the guard is not cosmetic), conditional `if IsSet { Remove; "Combat messages will no longer be gagged.\n\r"; return }; Set; "Combat messages will be gagged.\n\r"`. NOT XOR — the plan's adversary flagged that `^=` → `&^=` mutation passes a "toggle-off" test starting from flag-set state, so XOR has a mutation-testing blind spot; the conditional if-set-clear-else-set pattern does not. Line endings `\n\r` match every other `ch.Send` in the file. Divergences from C (documented in plan): (1) C has no standalone `do_gag` — the toggle is embedded in `do_config` at `src/act_info.c:5585-5833` with gag dispatched at `:5794-5795` (`bit = PCFLAG_GAG`); Go ships a standalone no-arg toggle matching the `DoAfk` local precedent, not C's `config +gag` / `-gag` syntax. (2) C echoes generic `"Ok.\n"` (the generic flag toggler has no knowledge of which flag); Go uses directional messages matching `DoAfk`'s convention. Registered in `internal/boot/boot.go` adjacent to `afk`: `Name: "gag"`, `Position: POS_DEAD` (matches C — mortal setting, no physical-state requirement), `Level: 0`. 4 tests in `internal/act/playercfg_test.go`: `TestDoGag_TogglesOn` (flag clear → set; output contains `"Combat messages will be gagged."`), `TestDoGag_TogglesOff` (flag set → clear; output contains `"Combat messages will no longer be gagged."`), `TestDoGag_NPCIsNoop` (`ACT_IS_NPC` set; no output, no panic — `TestDoAfk_NPCIsNoop` parallel), `TestDoGag_NilPCDataIsNoop` (non-NPC with `PCData == nil`; no panic, no output). Full mutation matrix from plan § G3 exercised via `Edit` round-trips only: `|=` → `&^=` on set path (TogglesOn fails with `"PCFLAG_GAG should be set after toggle"`); `&^=` → `|=` on clear path (TogglesOff fails with `"PCFLAG_GAG should be cleared after toggle"`); remove `IsNPC()` guard (NPCIsNoop fails — NPC emits output); remove nil-`PCData` guard (NilPCDataIsNoop fails with nil pointer panic); swap on/off messages (both toggle tests fail on output-content assertion). All 5 caught; all reverted cleanly. Persistence round-trip works with zero new code — the existing save/load code at `persist/player.go:243/520` already round-trips `PCData.Flags` as a bitvector. Doc citation corrections (G4): `TODO.md` line 117 `"Port DoGag player command (act_info.c:5795 — ~15 LOC; gag flag is currently test-only)"` → done-marker + correct citation `act_info.c:5585 do_config (gag branch at :5794)`; `smaug-go/doc/plan-dammessage-gaps.md:87` same citation correction, plus refreshed stale prose that incorrectly described `DoGag` as a follow-up (it's no longer a follow-up). Adversary review PASS on the code; CONCERNS on three doc-wrap-up items (TODO checkmark not set by worker, status header still "Planned" in plan file, stale prose in sister plan) — all three are manager-scope per the Document Management authority table and handled in this wrap-up. Files affected: `smaug-go/internal/act/playercfg.go` (+~25), `smaug-go/internal/act/playercfg_test.go` (+4 tests), `smaug-go/internal/boot/boot.go` (+1 register), `TODO.md` (2 line edits), `smaug-go/doc/plan-dammessage-gaps.md` (1 line + prose refresh), `smaug-go/doc/plan-do-gag.md` (status + completion record). `go test -count=3 ./...` green across all 15 packages. See completion record at the bottom of `smaug-go/doc/plan-do-gag.md`.

- **`rollD20` function-variable seam — landed (plan-rollD20-seam.md G1-G4).** `TestOneHitFull_ExplicitWieldUsed` at `internal/combat/combat_test.go:1981-2029` was flaking ~5% of full-suite runs before this landing. Root cause: the test calls `oneHitFull()` twice with `ch.Hitroll = 999` (intended as "always hit") and asserts `offhandDam > primaryDam`, but `rollD20()` can return `0` with probability `1/20`, and `combat.go:487` treats `diceroll == 0` as automatic miss (C-faithful — `src/fight.c:1541`). The flake is NOT a production bug: `rollD20() ∈ [0, 19]` and `0 = auto-miss` are intentional (`src/fight.c:1538` `while ((diceroll = number_bits(5)) >= 20)` yields `[0, 19]`). Both semantics match C; the test simply wasn't deterministic. Fix: `rollD20` at `combat.go:784` converted from `func rollD20() int { for { v := util.NumberBits(5); if v < 20 { return v } } }` to `var rollD20 = func() int { ... }` with the same body — joining the three existing function-variable seams in the file (`numberPercent` at :135, `oneHit` at :140-142, `oneHitOffhand` at :147-149). Zero production behavior change: single call site at `combat.go:487` resolves the same identifier; identifier resolution for `var` and `func` is identical at the call site. Seam doc comment on the var declaration records the no-`t.Parallel()` constraint (mutating a package-level `var` across parallel tests would race; none of the existing seam-using tests are marked parallel — the plan's Known Constraints section documents this for future authors). `TestOneHitFull_ExplicitWieldUsed` at `combat_test.go:1982-1987` now saves `rollD20`, installs a stub returning `10` (normal hit band — non-zero auto-miss, non-19 crit-hit; the plan's G4 math verified at level 50 `thac0=-30`, `Hitroll=999` gives `thac0=-1029`, victim `AC` capped at `-19`, so `thac0 - victimAC = -1010`, meaning any roll ≥ 1 hits — `10` stays safely within the normal-hit band), and restores via `t.Cleanup`. `Hitroll=999` comment refreshed from `// always hit` to `// large positive hitroll; combined with rollD20 stub above, guarantees hit`. A new `TestRollD20_IsSeam` at the end of `combat_test.go` pins the save/stub/restore pattern for future authors. `profbonus_test.go:255` gained a documenting comment (`// Deliberately unstubbed — tests statistical distribution; stubbing would defeat the test.`) above `TestOneHit_ProfBonus_HigherLearnedDealsMoreDamage` — the 2000-round statistical test is robust against the 1/20 variance, and stubbing would defeat the purpose. Worker mutation-verified by changing the stub from `10` to `0` (auto-miss sentinel) via `Edit`: test fails with `offhand dam=0 should exceed primary dam=0`; `Edit` back to `10`: 100/100 green. The plan specified `go test -count=100 -run TestOneHitFull_ExplicitWieldUsed ./internal/combat/...` as the mutation-verify cadence (false-pass rate at count=100 is 0.6% vs 21% at count=30 at a 5% flake rate — adversary flagged that count=30 is insufficiently sensitive, plan bumped to 100). With the stub: 100/100 green; without stub (Edit-reverted): observed 2-6 failures per 100-run depending on probe. Restored: 100/100 green. Adversary review PASS. One pre-existing unrelated flake in `TestViolenceUpdate_DualWield` observed once during the adversary's `-count=3` pass; five subsequent runs green — that test does not stub `rollD20` and is subject to the same auto-miss variance; tracked as a future seam application if the flake recurs. Files affected: `smaug-go/internal/combat/combat.go` (func → var, +expanded doc comment), `smaug-go/internal/combat/combat_test.go` (+~7 LOC stub setup in existing test + ~11 LOC new `TestRollD20_IsSeam`), `smaug-go/internal/combat/profbonus_test.go` (+1 comment line). `go test -count=3 ./...` green across all 15 packages. See completion record at the bottom of `smaug-go/doc/plan-rollD20-seam.md`.

- **Timer subsystem — landed (plan-timer-subsystem.md G1-G7).** Two-commit landing shipped the generic `handler.AddTimer` / `GetTimer` / `GetTimerPtr` / `RemoveTimer` / `ExtractTimer` / `DecrementTimers` subsystem (commit `8d5675b`) plus all four consumer wires (commit `062f4a7`). Closes the `TODO.md` combat-depth follow-up and activates `IsAttackSuppressed` which was previously functionally dead in production (the type existed, the read path existed, but nothing ever wrote to `ch.Timers` outside test fixtures). G1 ships new file `internal/handler/timer.go` with C-faithful upsert-by-type `AddTimer`, compact-in-place `DecrementTimers` with the universal `Value == -1` permanent-timer sentinel (matches C `fight.c:74-98` for `TIMER_ASUPRESSED`, generalized to all timer types per plan), nil-safe across all functions, and 20 tests with 5 worker mutations + a 6th adversary-run mutation all caught. Deliberate divergence from the plan prose: `DecrementTimers` is exported (capital D) rather than lowercase because the caller lives in the `combat` package; a lowercase name would not be reachable. Documented in the function's Go doc comment. G2 prepends a per-char decrement pass at the top of `combat.ViolenceUpdate` iterating ALL of `w.Characters` (not just fighters) — the plan adversary caught a 23x cadence bug in plan v1 that would have put the decrement in `charUpdate` (70s cadence) instead of `ViolenceUpdate` (3s `PULSE_VIOLENCE` cadence); putting it in the wrong loop would have made `TIMER_RECENTFIGHT` count=11 last ~770s instead of ~33s. G3 refactors `IsAttackSuppressed` to use `handler.GetTimerPtr(TIMER_ASUPRESSED)` with `Value == -1` permanent OR `Count >= 1` active semantics. Pre-existing `TestMultiHit_AttackSuppressedSkips` fixture had used `{Type: TIMER_ASUPRESSED, Value: 5}` with implicit `Count=0` — under the old contract any matching timer suppressed, so `Count=0` worked; under the new contract `Count=0` does NOT suppress, so the fixture was switched to `handler.AddTimer(ch, TIMER_ASUPRESSED, 5, "", 0)` (Count=5) to preserve test intent. G4 sets `TIMER_RECENTFIGHT` Count=11 on BOTH attacker and victim inside the existing PC-vs-PC block in `MultiHit`, AFTER the `PLR_NICE` early-return (so `PLR_NICE` path emits no timer on either side). Placement matches C `fight.c:982-986`; setting before any damage resolution guarantees the victim is still alive. G5 gates `DoQuit` on `!ch.IsNPC() && handler.GetTimer(ch, TIMER_RECENTFIGHT) > 0` with the C-faithful `"Your adrenaline is pumping too hard to quit now!"` message (C `act_comm.c:2875`) and `\n\r` line ending matching the rest of `info.go`. 8 new tests + `TestIsAttackSuppressed_CountOneBoundary` — the boundary test closes a coverage gap the first adversary caught (the `>= 1` vs `> 1` mutation was otherwise undetectable because `TestIsAttackSuppressed_PositiveCount` used Count=3). Every mutation applied by the worker + the boundary mutation added by the fix all caught. Two separate workers did inadvertent `git checkout -- combat.go` reverts during their own mutation-verification steps — lesson learned: manager will ban `git checkout` as a mutation-revert mechanism for future plans and use `Edit` round-trips instead. G6 updated the `mudprog/ifcheck.go:888` `pkadrenalized / asupressed` deferral comment to reference the now-existing `handler.GetTimer` / `GetTimerPtr` path for the follow-up wiring. Persistence scope cut: only `TIMER_PKILLED` persists in C (via `PTimer` in `save.c:546` — plan adversary caught v1 pointing at `db.c`); no Go code currently sets `TIMER_PKILLED`, so nothing persists today, and persistence lands alongside if/when PKilled mechanics port. Other scope cuts documented in `plan-timer-subsystem.md`: `TIMER_DO_FUN` callback dispatch (needs function-name registry), `TIMER_NUISANCE` / `TIMER_SHOVEDRAG` wiring (commands not ported), mudprog `timerskilled` / `asupressed` / `pkadrenalized` if-checks themselves (subsystem ready, wiring follow-up), deity prayer gate (`deity.c:1498`), wiz stat display of timer remaining. Files affected: `smaug-go/internal/handler/timer.go` (new ~140 LOC), `smaug-go/internal/handler/timer_test.go` (new ~230 LOC), `smaug-go/internal/combat/combat.go` (G2/G3/G4 ~+24/-11), `smaug-go/internal/combat/combat_test.go` (+~156 LOC), `smaug-go/internal/act/info.go` (+5 LOC G5 gate), `smaug-go/internal/act/info_test.go` (+~35 LOC `TestDoQuit_BlockedByRecentFight`), `smaug-go/internal/mudprog/ifcheck.go` (1-line comment update). `go vet ./...` clean. `go test -count=3 ./...` green across all 15 packages.

## 2026-04-17

- **`DoChannels` command — landed (plan-do-channels.md G2-G5).** Players can now toggle per-channel deaf bits via `channels -<name>` / `channels +<name>` / `channels -all` / `channels +all`; no-arg displays the grouped status listing with `+NAME` (enabled) / `-name` (deafened) markers. Required pair for the C-faithful `DoImmtalk` deaf-sender behavior that deliberately does NOT clear the deaf bit when a deafened immortal tries to immtalk (previously a permanent soft-lock in the Go port; `do_channels` is the C recovery path). Implementation lives in `internal/act/channels.go`: 30-entry `channelToggleTable` in C order including the non-obvious `muse → CHANNEL_HIGHGOD` mapping (NOT `CHANNEL_MUSIC`); NPC gate precedes everything else; `PLR_SILENCE` gates the no-arg path with the `"You are silenced.\n\r"` message; invalid-format path prints the C-exact `"Channels -channel or +channel?\n\r"`; unknown channel prints `"Set or clear which channel?\n\r"`; toggle success prints `"Ok.\n\r"`. All line endings are `\n\r` matching existing Go channel code + C source. `+all`/`-all` toggles the exact C public set (RACETALK, AUCTION, CHAT, QUEST, WARTALK, PRAY, TRAFFIC, MUSIC, ASK, YELL, plus AVTALK only when `Level >= LEVEL_IMMORTAL` — adversary-corrected from plan v1 which had listed `IS_HERO`). `pray` is omitted from the no-arg display (C `act_info.c:5358` has the line commented out) but the toggle table DOES include it and `channels -pray` works individually (match C toggle-table resolver at `5440-5441`). Display grouping mirrors C's four sections (Public / Private / Immortal / trust-gated) with `&g` / `&G` color codes; per-section gates are C-faithful (auction iff `GetTrust() > 4`; clan/council/guild iff the `PCData.Clan`/`Council`/`ClanType==CLAN_GUILD` predicate matches; avatar iff `Level >= LEVEL_HERO`; immortal section header + entries gated on `IsImmortal()`). Two deliberate simplifications flagged for follow-up: (1) immortal per-entry trust thresholds (`sysdata.muse_level` / `log_level` / `think_level` + hardcoded 57 for bug in C) collapsed to a blanket `IsImmortal()` gate pending a `sysdata` port; (2) `publicAll` slice at `channels.go:247` is a second place to maintain the public-channel set — low drift risk today but a candidate for deriving from `channelToggleTable` when the next channel gets added. 16 new tests in `internal/act/channels_test.go` cover NPC-noop, silenced-prints-message, no-arg-groups-display, `+CHAT`/`-chat` display casing, `+/-` toggle bit flips, invalid-format + unknown-channel error strings, `gossip`-keyword-rejected (documents `chat` fidelity), `muse → HIGHGOD` guard, `pray` display-vs-toggle discrepancy, `+all`/`-all` public-set coverage including an explicit mortal-at-LEVEL_HERO subcase that pins the `LEVEL_IMMORTAL` (not `LEVEL_HERO`) AVTALK gate, and the full end-to-end `TestDoChannels_RoundTripPersistsViaSave` that pipes `SavePlayer → bytes.Buffer → LoadPlayer` to prove G1 + G2 compose. Five mutations applied + reverted (toggle branches, AVTALK gate, muse-bit mapping, `-all` loop direction); every one caught by a specific named test before revert. Adversary independently ran a sixth mutation (inverting `fClear` at `channels.go:275`) with expected failure. `go vet ./...` clean. `go test -count=3 ./...` green across all 15 packages including no regression in Tier 9 (`DoImmtalk` / `DoGtell` / `BroadcastAuction` / `DoAuction`) and the pre-Tier-9 comm commands that got `PLR_SILENCE` gates. Registered in `internal/boot/boot.go` adjacent to the existing Tier 9 channel registrations (`channels` only — no alias, matching C). Files affected: `smaug-go/internal/act/channels.go` (+~200), `smaug-go/internal/act/channels_test.go` (+~290), `smaug-go/internal/boot/boot.go` (+1). Plan completion record appended to `smaug-go/doc/plan-do-channels.md`.

- **`Deaf` bitvector persistence — bug fix landed.** `plan-do-channels.md` G1 shipped standalone per the plan's suggested phasing. `SavePlayer` in `internal/persist/player.go` now emits a `Deaf` line conditional on non-empty, matching the `Act`/`AffectedBy` pattern immediately above it. The load path at `player.go:189-191` already consumed `case "Deaf":` via `ParseBitVector`, so the writer was the sole missing half of a round-trip. Before this fix, any player who toggled a channel (once `DoChannels` ships, or via any existing deaf-set path) silently lost the setting on every logout — adversary-flagged as the highest-impact finding in the audit. Two new tests in `internal/persist/player_test.go`: `TestSaveLoadPlayer_DeafRoundTrip` (two distinct channel bits survive a real `bytes.Buffer` round-trip with `Equal` check) and `TestSaveLoadPlayer_EmptyDeafNotEmitted` (forward-compat with old saves — the literal substring `"Deaf"` is absent when the bitvector is empty, and an old save without the line loads cleanly). Mutation-verified by wrapping the conditional in `if false` — round-trip test fails, revert, passes. Files affected: `smaug-go/internal/persist/player.go` (+3), `smaug-go/internal/persist/player_test.go` (+82). `go test -count=3 ./...` green. `DoChannels` command itself (plan G2-G5) follows in a second commit.

- **Pre-commit hook — landed.** Git pre-commit gate for smaug-go, modeled on `~/.claude/hooks/adversary-check.sh` but Go-only and simplified (no .py/.js/.ts branches) and exits non-zero on real problems so invoking agents can action the output. Hook script at `smaug-go/hooks/pre-commit` (tracked, not `.git/hooks/`), activated per-clone via `git config core.hooksPath smaug-go/hooks` driven by `smaug-go/hooks/install.sh` (idempotent) and wrapped by a new `make install-hooks` target; documented in the root `CLAUDE.md` *Build and run* section. Design pressure-tested by two parallel adversary subagents (correctness + usability angles) before implementation — both found issues that changed the shape of the hook. Correctness adversary caught that `go vet` / `go test` read from the working tree, not the index, so an unstaged edit could make the hook pass/fail against content different from what is being committed; mitigation is `git stash push --keep-index --include-untracked` with an `EXIT INT TERM` trap so the stash always pops, even on Ctrl-C or crash. Same adversary flagged that `git show :path | gofmt -l` reports `<standard input>` instead of the file path, so the hook writes each staged blob to a temp file whose basename matches the staged path before invoking `gofmt -l`, then prints a literal `Fix: (cd .../smaug-go && gofmt -w <files>)` command on failure; and that conflict-marker detection via grep on `git diff --cached` misdiagnoses deleted markers as additions, so the hook uses `git diff --check --cached` which distinguishes added lines from context and handles wholly-new files correctly. Usability adversary measured that `go test ./...` is 22.8s cold not the "~10s" initial estimate because `cmd/smaug/integration_test.go` boots a real server + telnet harness (22.4s of the total); splitting via `go list ./... | grep -v '/cmd/smaug$'` drops the hard gate to ~0.6s cached while `cmd/smaug` integration tests became a warn-only section runnable on demand via `SMAUG_RUN_INTEGRATION=1` (CI still covers them). Same adversary scanned existing code and enumerated legitimate no-test files (`internal/types/*.go`, `internal/net/{server,telnet,mssp,mccp,msdp}.go`, `internal/handler/{reset,find}.go`, `internal/testclient/{telnet,ansi}.go`, `internal/util/log.go`, `internal/combat/stance_index.go`) to demote the TDD sibling check to warn-only with exclusions for `package main` (read first 20 lines and grep `^package main\b`), `// Code generated` headers, rename status (`R`), and already-staged `_test.go` siblings. Hard gates wired: conflict markers, gofmt on staged blobs, `go vet ./...`, unit tests. Warnings: integration tests, missing test sibling, >150-added-line files, new TODO/FIXME/HACK/XXX, commented-out code heuristic. Escape hatches `SMAUG_SKIP_VET=1` / `SMAUG_SKIP_TESTS=1` added explicitly so `--no-verify` does not become reflexive — scoped bypasses preserve the cheap gates. Rebase detection (`$(git rev-parse --git-dir)/rebase-merge` or `rebase-apply` exists) skips all test gates during replay so `git rebase -i HEAD~20` stays under 2s total instead of 20 × 22s. Fast-path early-exit when staged diff contains no `.go` files — doc-only commits (CLAUDE.md, db/, src/) skip vet/test entirely. Output contract: `ERROR:` prefix for hard-gate failures always followed by a literal fix command, `[warn]` prefix for advisory findings; final line is `OK: pre-commit checks passed` or `BLOCKED: N check(s) failed`. Verified end-to-end against the install-then-run pipeline: `make install-hooks` → `core.hooksPath = smaug-go/hooks`; happy path (clean `.go` + sibling test) → exit 0; gofmt-violating file → exit 1 with the `gofmt -w` command embedded; synthetic conflict markers → exit 1 via `git diff --check` (3 detections); failing unit test → exit 1, `SMAUG_SKIP_TESTS=1 git commit` → exit 0; new `package main` file → no TDD warning (exclusion fired); doc-only stage → Go checks skipped, exit 0. Files affected: `smaug-go/hooks/pre-commit` (new, ~230 lines bash), `smaug-go/hooks/install.sh` (new), `smaug-go/Makefile` (+`install-hooks` target, +`.PHONY` entry), `CLAUDE.md` (+ one-line install note under *Build and run*). No code in `smaug-go/` changed; no tests changed.

- **Communication channels P1 — landed.** Four in-scope task groups from `smaug-go/doc/plan-channels.md` unblocking multi-player coordination. (G1) Shared infrastructure: `handler.IsSameGroup(a, b *CharData) bool` ports C `act_comm.c:4293-4301` with leaderless-self, nil-safety, and explicit flat-graph semantics (a 3-deep Leader chain does NOT collapse — C doesn't recurse either, and a dedicated test pins that behavior); plus the opportunistic sender-gate for `PLR_SILENCE` in `DoTell` / `DoYell` / `DoShout` / `DoGossip` — latent bug fix (`PLR_SILENCE` was set by `DoSilence` but no comm command consulted it; now matches C `talk_channel:500` `"You can't <verb>."` for channels and C `do_tell:1804` `"You can't do that."` for tells). (G2) `DoImmtalk` — C `act_comm.c:1327` port: mortal → `"Huh?"`; `PLR_SILENCE` sender gate; deaf-sender block `"You don't have the immtalk channel turned on..."` that deliberately does NOT clear the deaf bit (C's `xREMOVE_BIT` at `act_comm.c:514` is unreachable when the sender returns on `:511`); self-echo; broadcast over `WorldRef.Descriptors` with `CON_PLAYING && Trust >= LEVEL_IMMORTAL && !Deaf[IMMTALK]` filter; `DoClantalk`-style `&Y`/`&G`/`&D` color. (G3) `DoGtell` — C `act_comm.c:4217` port: empty-arg prompt, `PLR_NO_TELL` sender gate, walk `WorldRef.Characters` and deliver to everyone `IsSameGroup(gch, ch)`; added `Harness.QuickLoginTwo` in `internal/testclient/login.go` — the first helper/test in the suite that drives two simultaneously-logged-in clients, required so the channel pipeline can be tested through the real network path. (G4) `talk_auction` helper + stubbed `DoAuction`: `BroadcastAuction(message string)` exported for Phase-6 re-use with the full C `talk_auction:4309` filter chain (`CON_PLAYING && Trust >= 5 && !Deaf[AUCTION] && !InRoom.RoomFlags[ROOM_SILENCE]`); `DoAuction` emits `"The auction house is currently closed. (See the Phase-6 roadmap.)"` so the command visible in `commands` doesn't look broken — full auction state-machine deferred to Phase 6 (tracked in TODO.md). Alias parsing requires a space after `:` / `;` (`": hi"` works, `":hi"` does not — plan-documented limitation; `util.OneArgument` splits on whitespace and prefix-matching inside a token is a separate interpreter concern out of scope). Boot registers five entries (`immtalk`, `:`, `gtell`, `;`, `auction`) using the same-name-second-entry pattern from `pager`/`pagelen`. 26 new tests: 6 in `internal/handler/group_test.go` (IsSameGroup equivalence relation + nil + flat-graph); 5 in `internal/act/comm_test.go` (PLR_SILENCE gates across DoShout/DoGossip/DoYell/DoTell + NPC safety); 7 `TestDoImmtalk_*` in `internal/act/channels_test.go`; 6 `TestDoGtell_*` also there; 2 alias-dispatch tests exercising the registry (`: hi`, `; hi`); 8 in `internal/act/auction_test.go` (BroadcastAuction happy-path + trust/deaf/ROOM_SILENCE/non-PLAYING/nil-world + DoAuction stub/NPC-guard); 1 `TestGtell_TwoClients_Concurrent` in `internal/testclient/channels_test.go` as a full end-to-end verification. Every critical gate mutation-verified (flip the predicate → suite fails with specific test → revert → green): `IsSameGroup` leader-normalization; `DoImmtalk` `IsImmortal` check; `DoImmtalk` receiver `Trust >= LEVEL_IMMORTAL` gate; `DoGtell` `IsSameGroup` filter; `BroadcastAuction` trust gate. Files affected: `internal/handler/group.go` (new) + `group_test.go` (new); `internal/act/channels.go` (new — `DoImmtalk` + `DoGtell`) + `channels_test.go` (new); `internal/act/auction.go` (new — `BroadcastAuction` + `DoAuction`) + `auction_test.go` (new); `internal/act/comm.go` (PLR_SILENCE gates added to DoTell/DoYell/DoShout/DoGossip); `internal/act/comm_test.go` (+5 PLR_SILENCE tests); `internal/boot/boot.go` (+5 registrations); `internal/testclient/login.go` (+`QuickLoginTwo`); `internal/testclient/channels_test.go` (new); plan completion record appended to `smaug-go/doc/plan-channels.md`. `go test -count=3 ./...` green across all 15 packages.

- **Player-config commands P1 — landed.** Five commands + two util prereqs close four `TODO.md` items in one session. (G1) `DoSave` with level<2 gate, `ch.Wait=2`, `SaveFunc` invocation; (G2) `DoAfk` toggling `PLR_AFK` with self-message + `TO_CANSEE` room broadcast; (G3) `DoTitle` with `PCFLAG_NOTITLE` gate, 50-char truncation, tilde + color-token scrub, and the C `set_title` leading-space-for-alnum-start rule; (G4) `DoPassword` requiring `<old> <new> <again>` (divergence from C — C takes only `<new> <again>` with the old-pwd check commented out — chosen to align with the already-shipped bcrypt migration and meet a modern security baseline; min length bumped from C's 5 to 6); (G5) `pagelen` registered as a second name for the existing `DoPager` handler, verified via a dispatcher-level `Interpret` test. Util prereqs ported to C fidelity: `util.CaseArgument` (interp.c:1170, case-preserving first-word parse with quote handling) and `util.SmashColorToken` (db.c:4462, `&`→`+` and `^`→`-`). `act.BcryptCost` added as a package-local seam kept in sync with `game.BcryptCost` by `boot.Boot` so `TestOpts()` lowers both. 29 new test cases (28 in `internal/act/playercfg_test.go`, 1 in `internal/boot/boot_test.go` for the BcryptCost sync); 2 new util test cases in `internal/util/strings_test.go`. Every behavior gate mutation-verified (flip the gate → tests fail → revert → tests pass). Files affected: `internal/util/strings.go` (+`strings_test.go`), `internal/act/playercfg.go` (new), `internal/act/playercfg_test.go` (new), `internal/boot/boot.go` (+`boot_test.go`), plan completion record appended to `smaug-go/doc/plan-player-config.md`. `bio`/`description` stay deferred (blocked on `editor.go /s` bug — plan-player-config.md R6). `go test -count=3 ./...` green across all 15 packages.

## 2026-04-17 (earlier)

- Post-Phase-5 audit: 4 parallel adversary reviews (phase completeness, Go idiom, security + usability, C↔Go capability comparison). Overall verdict CONCERNS — no regressions, but documented combat-depth, config-command, and documentation-accuracy gaps. Findings in `smaug-go/doc/audit-2026-04-17.md`; follow-ups in `TODO.md`. Commit `720278d`.
- Post-audit batch 1 — 6 independent fixes (each test-first, adversary-verified): `mpForce` trust cap, `DoSaveArea` atomic write, `MaxAliases=50`, boot fail-loud on foundational subsystems, brute-force disconnect message wording, `TestSpellFarsight_Success` deterministic. Added positive-direction tests with mutation verification (`TestMpForce_PositiveDispatch`, `TestSpellFarsight_NPCTargetSucceeds`) and a `savesSpellStaffFn` seam in `magic/magic.go`. Files affected: `internal/act/{cmds2,olc}.go`, `internal/boot/boot.go`, `internal/game/loop.go`, `internal/magic/magic.go`, `internal/mudprog/commands.go` (+ tests). Commit `9b789a7`.
- **Combat depth — audit findings C1/C2/C3 resolved.** Nine-group plan landed: `MultiHit` extraction (G1), skill-check hook bridge `combat`←`act` with 18-gsn boot-time resolver (G2), PC multi-attack cascade second…seventh_attack (G3), dual-wield learned-roll + dual_bonus threading + low-move penalty (G4), front-of-round gates ACT_NOATTACK / PLR_NICE / AFF_BERSERK / TIMER_ASUPRESSED (G5), weapon proficiency bonus in `OneHit` non-ENABLE_WEAPONPROF branch (G6), stance application — NPC num_attacks stacking + PC GM bonus-attack loop + dam_done/dam_taken multipliers + synthetic StanceIndex since `db/system/stances.dat` is a stub (G7), dual-wield weapon alternation via explicit `oneHitFull(wield)` argument-passing replacing C's static `dual_flip` race (G8), docs (G9). 12 acceptance criteria verified with RNG-stubbed deterministic tests — no loose statistical checks. Files affected: `internal/combat/{combat,combat_test,skillcheck,skillcheck_test,profbonus,profbonus_test,stance_index,stance_test,scenario_test}.go`, `internal/combat/combat.go`, `internal/boot/{boot,boot_test}.go`, `internal/act/{skills,consume,skills3}.go`. Deferred: `handler.AddTimer` for `TIMER_RECENTFIGHT` wiring, devoted-clan favor penalty in prof bonus, move-cost-per-round tracking. `go test -count=3 ./...` green across all 15 packages. See completion record at the end of `smaug-go/doc/plan-combat-depth.md`.
