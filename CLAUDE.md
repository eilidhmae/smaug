# SMAUG MUD

## Prime Directives (override all other rules)

1. **Assume the manager role defined in `@.claude/agents/manager.md` and follow its Prime Directives in full — they carry equal weight to the ones here.**
2. **On session start, read these foundation docs to resume context:**
   - `@smaug-go/doc/plan.md` — architecture, C→Go mapping, testing strategy
   - `@smaug-go/doc/phases.md` — phase roadmap and overall status
   - The most recent phase record (see the index under *Active Work* below)
3. **Consult other `smaug-go/doc/` files on demand via the index — do not preload the whole directory.**

---

SMAUG (Simulated Medieval Adventure Multi-User Game) — a text-only MMORPG on the Diku → Merc → SMAUG lineage. Active work is the pure-Go port in `smaug-go/`; original C in `src/` is reference only. Game data lives in `db/`. Current phase status is in `smaug-go/doc/phases.md`.

## Active Work: C → Go port

### Documentation index (`smaug-go/doc/`)

Read foundation docs every session; fetch phase records only when touching that phase; consult review docs on demand.

**Foundation:**

| File | Contents |
|---|---|
| `plan.md` | Architecture, C→Go mapping, design decisions, testing strategy |
| `phases.md` | All 5 phases: deliverables, verification criteria, current status |
| `post-phase6-vision.md` | Post-Phase-6 modernization capture — SOGI gender, poly marriage, Heritage+Community (Daggerheart). **Read before touching `Sex` / `Race` / `Spouse` fields or `util.Act` pronoun tokens.** Captures user-stated direction; not a plan. |

**Phase records:**

| Phase | Plan / scope | Completion record |
|---|---|---|
| 1 | — | `phase1-completed.md`, `phase1-remaining.md` |
| 2 | `phase2-remaining.md` | `phase2-completed.md` |
| 3 | `phase3-plan.md` | `phase3-completed.md` |
| 4 | `phase4-plan.md` | `phase4a-completed.md`, `phase4b-completed.md` |
| 5 Tier 1 (Act/spell_smaug/saves/persistence) | `phase5-tier1-foundation.md` | `phase5-tier1-completed.md` |
| 5 Tier 2 (flag honoring + idle-data wiring) | `phase5-tier2-wiring.md` | `phase5-tier2-completed.md` |
| 5 Tier 3 (mudprog depth) | `phase5-tier3-mudprog.md` | `phase5-tier3-completed.md` |
| 5 Tier 4 (content breadth) | `phase5-tier4-content.md` (plan + completion) | — |
| 5 Tier 5 (test client + boot extraction) | `phase5-tier5-testclient.md` (plan + completion) | — |
| 5 Tier 6 (combat depth — multi-attack + prof bonus + stance) | `plan-combat-depth.md` (plan + completion) | — |
| 5 Tier 7 (dammessage gaps — was_in_room + PCFLAG_GAG + poison prefix) | `plan-dammessage-gaps.md` (plan + completion) | — |
| 5 Tier 8 (player-config commands — save / afk / title / password + pagelen) | `plan-player-config.md` (plan + completion) | — |
| 5 Tier 9 (communication channels — immtalk / gtell / auction stub) | `plan-channels.md` (plan + completion) | — |
| 5 Tier 10 (Deaf persistence fix + DoChannels toggle command) | `plan-do-channels.md` (plan + completion) | — |
| 5 Tier 11 (Timer subsystem + IsAttackSuppressed wiring + TIMER_RECENTFIGHT + DoQuit gate) | `plan-timer-subsystem.md` (plan + completion) | — |
| 5 Tier 12 (editor `/s` save — `EditorSave` callback + `CON_PLAYING` transition) | `plan-editor-save.md` (plan + completion) | — |
| 5 Tier 13 (`DoGag` standalone toggle) | `plan-do-gag.md` (plan + completion) | — |
| 5 Tier 14 (`rollD20` function-variable seam — eliminates combat test flake) | `plan-rollD20-seam.md` (plan + completion) | — |
| 5 Tier 15 (Tranche A quick wins — `DoBio`/`DoDescription`, `[AFK]` on who, `%X` prompt, retcode guards, testclient idiom polish, publicAll derivation) | — (ran without dedicated plan doc) | CHANGELOG.md 2026-04-18 entry |
| 5 Tier 16 (Tranche B — stances loader + DoMset stance branch + TIMER_DO_FUN callback registry + 7 mudprog if-checks + wordlist match + Oprog/Rprog CMD port) | `plan-tranche-b.md` (plan + completion) | — |
| 5 Tier 17 (Tranche C — quality/fidelity: `util.Act` per-call AType color + 33-site migration, `update_aris` audit correction, `PLR_BLANK` blank-line emission, XP-on-skill-gain + adept-cap message) | `plan-tranche-c.md` (plan + completion) | — |

**Phase 5 is complete (2026-04-18).** All 17 tiers + Tranches A/B/C landed; last commit `26db5f9`. **Phase 6 planning underway** — see `smaug-go/doc/phase6-roadmap.md` for the full candidate inventory, dependency graph, and recommended 6-wave execution order. The roadmap was externally audited 2026-04-18; 21 factual corrections applied (several "missing schema" claims were wrong — `MorphData`, `CharData.X/Y/Map/Sector`, `RoomIndexData.Plane`, `WEAR_MISSILE_WIELD`, `ITEM_PROJECTILE/QUIVER` are already defined).

**Phase 6 plans authored (all audited as of 2026-04-19):**

| Plan | Status | Scope |
|---|---|---|
| `plan-phase6-arena.md` | **LANDED 2026-04-19** | Arena PvP — `challenge`/`accept`/`decline`/`withdraw` + arena-victory + challenge-timeout tick. 7 task groups (G1 schema + G2-G4 commands + G5 charUpdate tick + G6 ArenaVictoryCheck + G7 boot wire/E2E), 15 criteria all satisfied. New `internal/act/arena.go` (310 LOC) + `internal/act/arena_test.go` (37 unit tests, all gates) + `internal/combat/arena_victory.go` (130 LOC) + `internal/combat/arena_victory_test.go` (11 tests including Damage integration) + `internal/testclient/arena_test.go` (3 E2E scenarios using `QuickLoginTwo` — first end-to-end two-player PvP in the test suite). `internal/handler/timer.go` gains `HasTimer` (3-line wrapper). `internal/types/constants.go` adds `ROOM_VNUM_ARENA_MIN=10366`/`MAX=10382` (`src/mud.h:2283-2284`). `internal/types/enums.go` adds `TIMER_CHALLENGE=8`. `internal/persist/player.go` gains AKills/ADeaths round-trip (emit-when-nonzero keeps pfiles clean for non-arena players). `internal/combat/skillcheck.go` extends gsn-resolve with gsnPoison/Blindness/Sleep/Curse for arena-debuff strip. Boot wires `combat.ArenaIsBusyFunc = act.SetArenaIsBusy` and `combat.DoLookFunc = act.DoLook`. **Area-data edit committed**: `db/area/newacad.are` got `ROOM_ARENA` added to 17 rooms (10366-10382); drift-prevention test `TestBoot_ArenaRoomsFlaggedInShipped` in `internal/boot/boot_test.go` regex-scans the shipped file to catch OLC-roundtrip regressions. **Open-Q resolutions** (from plan): Q1 `do_decline` C bug preserved verbatim (clears ACT_CHALLENGER on decliner, leaves challenger's flag until 5-tick timeout); Q2 per-player timer via `handler.AddTimer`; Q3 pickArenaRoom filters by flag then randoms the valid set (not C-verbatim); Q4 Level 0 / POS_RESTING on all 4 commands; Q5 altar-to-temple fallback at G6 if altar vnum unloaded; Q6 area-file edit; Q7 stop_fighting inside helper. **C bug preserved verbatim**: `do_decline` flag-clear target; `do_accept` calls `do_look(ch)` twice (accepter sees second look, victim none). **6 mutation gates verified** via `Edit` round-trips only (drop ACT_CHALLENGER set, weaken level gate `<=5`→`<5`, drop charUpdate flag-clear, swap temple/altar in arena victory, drop AKills++, drop arena intercept in Damage — each fails its pinned test). `go build ./...` clean, `go vet ./...` clean, `gofmt -l .` clean on all arena files, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per tooling caveat. Commit `0cc1d97`. |
| `plan-phase6-hotboot.md` | **LANDED 2026-04-19** (executable plan authored + pre-execution audit CONCERNS with 6 corrections applied + 4-adversary post-execution review completed with 5 fixes applied before commit 2026-04-19; Q1 resolved same-day to Linux/Unix-only via `//go:build !windows`) | Seamless server restart via `syscall.Exec` + FD inheritance (`net.FileListener` + `net.FileConn`). 8 task groups (pre-G0.a `HomeVnum` wiring + pre-G0.b `SysData.HotbootInProgress` + G0–G7), all 18 acceptance criteria satisfied, 16 mutation gates verified via `Edit` round-trips only, 60+ new tests including `TestHotboot_EndToEnd` integration under `//go:build integration` (real-process spawn; measured **~9.4s real hotboot pause** in G6 — dominated by fresh-image area/data reload per C parity). New `internal/persist/hotboot.go` + `hotboot_sessions.go`, `internal/act/hotboot.go`, `internal/net/hotboot_pause.go`, `internal/boot/recover.go` (all `//go:build !windows`); 5 Windows stub files + `cmd/smaug/hotboot_argv_windows.go`; new `make windows-build-check` Makefile target pins A17. C bug at `hotboot.c:675` fixed (drop-message to `d`). **Post-execution adversary review (4 parallel agents)** applied 5 fixes: (1) BLOCKING data race in `ResumeFromPause` fixed via `acceptWG sync.WaitGroup` — `go test -race -count=5 ./internal/net/` clean; (2) HIGH OLC silent-drop fixed — `DoHotboot` gate extended to refuse `CON_REDIT`/`CON_OEDIT`/`CON_MEDIT` with new `TestDoHotboot_RejectsAnyOLCSubstate` (3 subtests); (3) HIGH `descFiles` FD leak on `SaveHotbootSessions` failure fixed; (4) MEDIUM security 0644→0600 on all `db/hotboot/*.dat` files with `TestSaveWorld_FileMode`+`TestSaveHotbootSessions_FileMode`; (5) MEDIUM ops `[hotboot] recovery complete: N of M sessions restored` summary log in `BootRecover`. 3 deferred non-blocking follow-ups tracked in `TODO.md`: pre-existing `TestBio_RoundTripThroughEditor` race under `-race` (unrelated to hotboot), supervisor/Docker deployment notes, symlink TOCTOU threat-model doc. Commit `TBD` (orchestrator owns). |
| `plan-phase6-starmap.md` | **LANDED 2026-04-18** | `look sky` port — 3 groups, 13 criteria (A11 satisfied with documented fidelity deviation — eclipse render is a black-disk `" "` patch, not `&W@`, per C moonphase=0 masking). `internal/act/starmap.go` + `starmap_test.go` new (250 + 628 LOC); `internal/act/info.go` gains the `"sky"` branch after bare-arg room render and before keyword match. 8x72 `starMap` + 3x5 `sunMap` / `moonMap` verbatim from `src/starmap.c:59-85`. File-private constants (`starmapWeathUnit=10` etc.). Indoor check uses flag-OR-sector canonical pattern (`ROOM_INDOORS` OR `SECT_INSIDE`) matching `spell_unique.go:281-282` / `ifcheck.go:640-643`. 37 new tests green over `-count=3`, 7 `Edit`-round-trip mutations all caught (NUM_DAYS, weath_unit, byte-in-starMap, drop SECT_INSIDE clause, drop moonphase clamp, sun color, line-terminator). Commit `cd6f1b3`. |
| `plan-phase6-holidays.md` | **LANDED 2026-04-19** | Holiday CRUD + persistence + `month_name[]` port — 4 groups, 15 criteria all satisfied (A1–A13 + A14/A15 from readiness vet). `LoadHolidays`/`SaveHolidays` in new `internal/persist/holidays.go`; `DoHolidays` (Level 0) / `DoSaveHoliday` (LEVEL_ASCENDANT=60) / `DoSetHoliday` (LEVEL_ASCENDANT) + `GetHoliday` + `firstToken`/`preserveCaseRest`/`findHoliday` helpers in new `internal/act/holidays.go`; `MonthNames[17]` + `MonthName` in new `internal/types/months.go`; `HolidayData` in new `internal/types/holiday.go`. Boot wire at `internal/boot/boot.go` adds loader + `SysData.{MaxHoliday,MonthsPerYear,DaysPerMonth}` defaults + 3 registrations; `internal/act/info2.go` `DoTime` gains holiday-today suffix. **3 latent C bugs fixed, all pinned**: `<= 1` day/month rejections replaced with `< 1`; `create` stores `TimeInfo.Month+1`/`Day+1` instead of unshifted 0-indexed. **Q6 partial resolution**: `GetHoliday` (A14) + `DoTime` suffix (A15) landed; season-tick auto-broadcast (C `src/timezone.c:617-631`) deferred to TODO.md — `weatherUpdate` has no `echo_to_all`/`season_update` equivalent. C divergences documented inline: shipped-file implicit-`#END`-at-EOF (no bug log), name case preservation via `firstToken`, multi-word `announce` and `name`, clean month-list fallback. 11 mutation gates verified via `Edit` round-trips only (banned-list respected). 55 new tests across 4 packages. `go build ./...` clean, `go vet ./...` clean, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. Commit `04366ad`. |
| `plan-phase6-marriage.md` | **DEFERRED 2026-04-19** (human decision — park; revisit on demand signal) | marry/divorce/rings — 5 groups (G1-G5), 12 criteria. Audited 2026-04-18 (CONCERNS): `newgate.are` vnum-100/101 collision invalidates original nil-guard strategy for `DoRings`. Parked rather than forcing Q2 resolution among the 4 options. |
| `plan-phase6-planes.md` | **LANDED 2026-04-19** | Planes CRUD — 3 groups, 13 criteria. `LoadPlanes`/`SavePlanes`/`CheckPlanes` in new `internal/persist/planes.go` + `DoPlist`/`DoPstat`/`DoPset` + `planeLookup` in new `internal/act/planes.go`; boot wire registers three commands (Level 0 / LEVEL_IMMORTAL / LEVEL_GREATER) and calls `CheckPlanes(w, nil)` after loader. A1 empty-slice-vs-nil distinction implemented (missing file → `(nil, nil)`; stub `#END\n` → `(non-nil empty slice, nil)`). Tilde-tolerant Name reading accepts both C-format (space-terminated) and SMAUG-format (tilde-terminated); saver always emits tilde. Splice-delete algorithm (post-audit correction) handles first/middle/last index uniformly — `TestDoPset_DeleteMiddlePlane` + `TestDoPset_DeleteFirstPlane` + `TestDoPset_DeleteLastPlaneSymmetric` + `TestDoPset_DeleteLastPlaneRebuildsPrime` pin all four cases. SmashTilde on rename is deliberate C-divergence (C doesn't smash). 10 mutation gates verified via `Edit` round-trips (tilde-strip, name-empty rejection, Prime Material seed, deleted-pointer reassign, planeLookup pass-2, HasPrefix arg order, SmashTilde, CheckPlanes-after-splice, three registrations, boot CheckPlanes call). 36 new tests across `planes_test.go` files (persist: 19, act: 23, boot: 4, testclient E2E: 3). `go build ./...` clean, `go vet ./...` clean, `go test -count=3 ./...` green across all 15 packages. Commit `d895e5b`. |
| `plan-phase6-channels-extra.md` | **LANDED 2026-04-18** | `music`/`racetalk`/`wartalk`/`counciltalk`/`guildtalk`/`newbiechat` — shared `talkChannel` helper + 6 thin wrappers. 6 groups, 18 criteria, all satisfied. 7 mutation gates verified via `Edit` round-trips (wartalk/yell exception, self-echo-no-rebroadcast, filter application, pkill/council/guild/newbie gates, case-insensitivity). Helper in `internal/act/channels.go` owns PLR_SILENCE / deaf-sender (wartalk+yell exception with C's `||` bug corrected to `&&`) / ROOM_SILENCE / self-echo / broadcast; per-channel `recipientFilter` closures supply audience predicate. Tier 9 channels kept open-coded (non-retrofit). +~160 LOC impl, +~700 LOC tests. Stock `db/councils/council.lst` empty → `newbiechat` immortal-only by default matches C. Commit `5ff02b6`. |
| `plan-phase6-skills.md` | **LANDED 2026-04-18** | bloodlet/pounce/broach — 3 groups, 16/16 criteria satisfied. Ported `DoPounce` (`internal/act/skills3.go`), `DoBroach` (`internal/act/skills.go`), `DoBloodlet` + `isBloodRace` helper (`internal/act/skills4.go`), plus 3 `boot.go` registrations. 41 new tests all green (`-count=3`). Both C bugs fixed per Option-A (bloodlet operator-precedence: `!IS_NPC && (vampire || demon)`; broach tautology: `CLOSED && LOCKED && !PICKPROOF && can_use`). Bloodlet Wait uses literal `PULSE_VIOLENCE` not `Beats` per C :3535. Mutation-verify round-trips confirmed 5 mutations (weapon-type set, broach predicate, bloodthirst decrement, Wait constant, reverse-exit removal). Commit `1d929e8`. |
| `plan-phase6-stances-olc.md` | **LANDED 2026-04-19** | Combat Stances OLC completion — 5 task groups (G1 struct extension + loader upgrade + helpers; G2 CanUseStance + UpdateStances + DoStance rewrite; G3 SaveStances writer + round-trip; G4 flag helpers + DoSTstat + DoSTset; G5 boot wiring + scenario tests), 20 acceptance criteria all satisfied. **Q2 decision: defensive no-op (preserve C bug)** — class/race setter bodies absent in C, Go matches verbatim. Closes live gameplay divergence: pre-G2 `DoStance` was a 7-name stub with NO prereq check, NO mount guard, NO change-while-active gate, NO update_stances, NO wait state — players selected DRAGON with zero mastery. New files: `internal/combat/can_use_stance.go` (88 LOC), `internal/combat/stance_update.go` (33 LOC), `internal/persist/stances_save.go` (145 LOC), `internal/util/flags.go` (RisflagNames 22-entry + GetRisflag + FlagString), `internal/act/stances_admin.go` (237 LOC — DoSTstat 13-line display + DoSTset 18-field editor). Modified: `internal/combat/stance_index.go` (StanceInfo 3→19 fields + 4 helpers GetStanceName/GetStanceMastery/GetSpecialName/GetSpecialNumber), `internal/persist/stances.go` (loader stores all 18 fread_stance keys; adds `StancePath` package var for `stset save` round-trip), `internal/act/skills4.go` (DoStance rewrite with sendStanceMessage helper), `internal/boot/boot.go` (+2 registrations ststat/stset at LEVEL_IMMORTAL POS_DEAD). **4 C bugs preserved verbatim** (class setter no-op at stances.c:823-826, race setter no-op at :918-921, dual inverted at :858-861, can_use_stance `IS_SET(mask, index)` quirk at :276-279) — each pinned by dedicated TestDoSTset_*_CBugPreserved or TestCanUseStance_*Restriction_CBug_* test. **1 cosmetic C bug fixed**: `do_ststat` newline-every-4 — C `:687` operator precedence bug (`!index_num % 4`) fires never; Go ports intent `index_num % 4 == 0`. **1 C divergence**: `sendStanceMessage` empty-string nil-guard (C calls `act` with potentially-NULL *others/*self — segfaults). **Type-mismatch correction honored**: CharData.Stance* (int) vs RIS_* (uint32) vs StanceInfo.{Resist,Immune,Suscept} (int) — explicit `int(types.RIS_FIRE)` casts at fixture sites; no cast needed inside UpdateStances. RisflagNames table = 22 entries terminating in magic/paralysis per `constants.go:196-217` (audit-corrected from plus7-stopping draft). `stset save` path resolution: `persist.StancePath` (set by loader) → `WorldRef.DataDir/system/stances.dat` fallback. **WAIT_STATE uses `util.UMAX`** (preserve pre-existing higher wait). **TestDoStance_Set updated** from `dragon`→`viper` per readiness-vet (VIPER's Prereq[0]==0 satisfies CanUseStance without fixture seeding). **8 mutation gates verified** via `Edit` round-trips only (banned-list respected): drop Dodge storage, flip STANCE_NONE sentinel, flip UpdateStances `|=`→`&=`, flip MaxWeight `>`→`>=`, drop change-while-active gate, flip DamDone `>0`→`!=0`, drop EndStance blank-line, flip attacks `>5`→`>4`. 109 new tests across 9 files. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean on all changed/new files, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. Commit `4af9823`. |
| `plan-phase6-auction.md` | **LANDED 2026-04-19** | Full `do_auction` state machine (non-GSC variant) — 10 task groups, 23 acceptance criteria all satisfied. Completes Tier-9 `BroadcastAuction` stub by porting C `act_obj.c:3775-4240` + `update.c:3183-3326`. New `pulseAuction` slot in `GameLoop` (initialized in `NewGameLoop` struct literal per readiness-vet — does NOT repeat pre-existing `pulseSave` uninitialized bug at `loop.go:68-94`); `internal/util/parsebet.go` (`ParseBet`/`Advatoi` — bare-digit + k/m multipliers + `+N%` percent + `*N`/`xN` multiply); `internal/util/numpunct.go` (`NumPunct` comma-thousands formatter); `internal/persist/noauction.go` (line-per-vnum loader with `0`-sentinel terminator — missing-file permissive, malformed-line `util.Bug`-and-skip); `internal/handler/economy.go` (public `BoostEconomy`/`LowerEconomy` promoted from `mudprog` package-private). `AuctionData` extended with `History [AUCTION_MEM]*ObjIndexData` + `HistTimer int`. `world.New` eagerly initializes `Auction: &AuctionData{}` + new `NoAuction []int` field. **Two-layer disconnect defense** per §D8: `DoQuit` gate ported verbatim from C `act_comm.c:2883-2890` (`"Wait until you have bought/sold the item on auction."`); `closeDescriptor.clearAuctionOnDisconnect` Go-originated defensive clear with seller-drop-to-room + refund-distinct-buyer + buyer-drop-reverts-bet branches — catches socket-level disconnects that bypass DoQuit. **Tax math** per §Q7 port-with-fix: `pay := bet*9/10; tax := bet - pay` preserves `pay+tax == bet` invariant (C's `(int)(bet*0.9)+(int)(bet*0.1)` evaporates 1 gold on `bet=11`). **Scope-documented deferrals**: `do_noauction` admin CRUD at `act_wiz.c:11120-11174` (load-only port shipped, command tracked as TODO); `ms_find_obj` drunk/mental-state gate ported as no-op consistent with existing Go omission across get/drop/put/quaff/recite/eat/drink; carry-weight cap on obj-to-char overflow (Go has no weight subsystem yet — always give to recipient, plan §Q4); GSC gold/silver/copper variant fully out of Phase 6. **8 mutation gates verified** via `Edit` round-trips only (banned-list respected): +10000 step → +1000 (bid test fails); NumPunct modulo drop (panic on slice OOB); time-gate `||→&&` (night-hours test fails); history rotation direction flip (rotation test fails); tax split `9/10 → 1/10` (sold-branch test fails with inverted gold split); disconnect seller-branch body removed (cancel test fails); DoQuit gate `Item!=nil → Item==nil` (both blocked-quit tests fail); noauction 0-sentinel `break → continue` (sentinel test fails). 86 new tests across 7 files. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. Commit `d82d632`. |
| `plan-phase6-clan-officer.md` | **LANDED 2026-04-19** | Clan officer commands — induct / outcast / bestow + `SaveClan` bundled as G0. 5 task groups, 14 acceptance criteria all satisfied (G5 rank-management deferred per Q1 Option 2). **Q1 decision: DEFER** — rank management stays gated behind a future `plan-phase6-setclan.md`; officers can induct/outcast but not promote. New files: `internal/act/clan_officer.go` (445 LOC — `isClanOfficer` 5-way gate, `isPkill`, `officerRank`, `echoToPKers`, `DoInduct`/`DoOutcast`/`DoBestow`), `internal/act/clan_officer_test.go` (~1100 LOC, 68 tests). Modified: `internal/persist/subsystems.go` (extract `readClan(sc)` shared helper; add missing loader keys `Abbrev`/`LeadRank`/`OneRank`/`TwoRank`/`MemLimit`/`ClanObjFour`/`ClanObjFive`/`PKillRangeNew`/`PDeathRangeNew`/legacy `PKillRange`/`PDeathRange` 7-int consume-and-discard; fix pre-existing `PKills[0]→PKills[6]` legacy-slot bug to match C `fread_clan:434-435`; add `SaveClan(w io.Writer, c *ClanData) error` + `SaveClanFile(dir, c)` helpers byte-for-byte parity with `src/clans.c:206-250`), `internal/persist/subsystems_test.go` (11 new tests: `TestLoadClan_LegacyPKillsIndex6` pins the bug fix, `TestLoadClan_PKillRangeNew_SevenInts`/`_Legacy_Discarded` pin block semantics, `TestLoadClan_AllExtendedKeys`, `TestSaveLoadClan_RoundTrip`, `TestSaveClan_FormatMatchesGolden` pins byte-for-byte C parity, `TestSaveClan_SmashTildeOnStrings`, `TestSaveClanFile_WritesToPath` / `_NilRejected` / `_EmptyFilenameRejected`, `TestSaveClan_NilRejected`), `internal/boot/boot.go` (+5 LOC: export `act.ClanDir = clanDir` for SaveClanFile persist seam + register 3 commands at `induct`=Level 0/POS_RESTING, `outcast`=Level 0/POS_RESTING, `bestow`=LEVEL_IMMORTAL/POS_DEAD), `internal/boot/boot_test.go` (+1 `TestBoot_ClanOfficerRegistered`). **Authority design**: `induct`/`outcast` registered at Level 0 because authority is the in-command `isClanOfficer` 5-way gate (bestowed-keyword, deity, leader, number1, number2), not a trust-level gate — matches C `tables.c:752-753,1151-1152`. A low-level PC who is their clan's leader CAN induct; a high-level PC who is not an officer cannot. `bestow` is the immortal-only arm at LEVEL_IMMORTAL. **C bugs preserved verbatim**: (1) `do_induct`'s "already belongs to an guild" grammar (article `an` before `guild` — `clans.c:1039`) — Go ports the literal string, pinned by `TestDoInduct_AlreadyInAGuild`; (2) `do_bestow`'s leading-space-on-first-append quirk (`"" + " " + "induct" = " induct"`) — Go port matches, pinned by `TestDoBestow_Append` + `TestDoBestow_IsNameDetectsLeadingSpace` which verifies `util.IsName` parses through the leading blank; (3) `do_outcast`'s self-outcast messages at `clans.c:1238-1255` are structurally DEAD CODE — when `victim == ch`, the power gate `x<=y && trust<=trust` at `:1231` always fires first because x/y both derive from the same name and trust equals trust; Go preserves the branches verbatim, `TestDoOutcast_SelfOutcast_PowerGateBlocks` documents this observation. **Pre-existing bug fixed**: `internal/persist/subsystems.go:66-69` stored legacy-form `PKills`/`PDeaths` into `clan.PKills[0]`, but C `fread_clan:434-435` stores into `[6]` (cumulative slot); Go now targets `[6]` and pinning test `TestLoadClan_LegacyPKillsIndex6` hard-asserts both `[6]==42` AND `[0]==0`. **Scope cuts preserved** (plan §Scope Cuts): `do_promote`/`do_demote` don't exist in C (roadmap error); `do_setclan` full 30-field surface is a separate plan; `do_bestowarea` immortal areafile bestowal is a separate plan; `save_member_lists`/`add_member`/`remove_member` per-clan roster file is display nicety; `add_loginmsg` for linkdead outcast victim (`util.Bug` logs instead — victim learns on next login by seeing `PCData.Clan == nil`); `save_char_obj(victim)` in `DoBestow` intentionally NOT called to match C semantics (bestowments persist on next pfile save). **6 mutation gates verified via `Edit` round-trips only** (project banned-command list respected — no `git checkout`/`git restore`/`git stash`/`git reset --hard`): (1) flip `victim.Speaks |= LANG_CLAN` to `&^=` in DoInduct → `TestDoInduct_Success_PkillClan_SetsFields` + `_NokillClan_SetsLangClanButNotDeadly` both fail; (2) swap `officerRank(Leader)` return `3→0` → `TestDoOutcast_RankArithmetic_OfficerOutrankedByVictim` + `_VictimHigherLevel` both fail; (3) swap DoBestow `"none"` branch assignment from `""` to `argAfter` → `TestDoBestow_None` fails; (4) swap `clan.Members--` to `++` in DoOutcast → `TestDoOutcast_Success_ClearsFields` fails; (5) swap `victim.Level < 10` to `<= 10` → `TestDoInduct_VictimLevelExactly10_Allowed` fails (boundary test added specifically for this gate); (6) revert PKills legacy-slot back to `[0]` → `TestLoadClan_LegacyPKillsIndex6` fails on all four index assertions. 68 new tests across clan_officer_test.go + 11 new tests across subsystems_test.go + 1 boot test + update to `TestLoadClan_AllFields` reflecting the PKills-index-6 semantics (test was previously asserting the bug). `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean on all changed files, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. Commit `fc4dfc8`. |
| `plan-phase6-olc-redit.md` | **LANDED 2026-04-19** | Interactive Room Editor (`CON_REDIT`) — 13 task groups, 18 acceptance criteria all satisfied. First of three OLC-substate plans; establishes the nanny-dispatch pattern + `OlcData`-on-descriptor allocation + `CON_REDIT` loop arm that `oedit`/`medit` will inherit. New files: `internal/types/olc.go` (OlcData struct + 24 REDIT_* mode constants iota+100), `internal/types/olc_test.go` (4 tests pinning struct fields / mode uniqueness / CON_* separation), `internal/game/redit_menu.go` (menu renderers — main, flag, sector, exit, exit-edit, exit-flag, exit-dirs, extradesc, extradesc-choice — plus r_flags table verbatim from `src/build.c:92-105`, sector labels, directionName, exit-flag labels), `internal/game/redit_parse.go` (top-level reditParse dispatcher + per-mode handlers for all 24 REDIT_* modes + cleanupOlc + olcLog + worldRef seam via SetWorldRef), `internal/game/redit_parse_test.go` (34 unit tests covering main menu, flags word+number paths, sector validator, tunnel URANGE clamp, exit add/edit/delete/vnum/key/keyword/desc/flags, extradesc add/junk/keyword, cleanupOlc invariants, helpers), `internal/testclient/redit_test.go` (3 E2E scenarios: menu-entry-and-quit, set-name, invalid-choice-redisplays). Modified: `internal/types/descriptor.go` (add `Olc *OlcData` field; nil-default pinned by test), `internal/game/loop.go` (add `case types.CON_REDIT: reditParse(d, line)` arm to processInput dispatch switch — pre-existing default-nanny fallthrough handled bare CON_REDIT before this), `internal/act/olc.go` (extend `DoRedit(ch, "")` no-arg path: allocates `ch.Desc.Olc`, sets Connected=CON_REDIT, calls `ReditDispMenuFunc` — flat subcommand path untouched; add `ReditDispMenuFunc` seam declaration), `internal/boot/boot.go` (wire `act.ReditDispMenuFunc = game.ReditDispMenu` + `game.SetWorldRef(w)`), `internal/act/olc_test.go` (replace `TestDoRedit_NoArg` with `TestDoRedit_NoArgEntersMenu` + `TestDoRedit_WithArgKeepsFlatPath` — pin new behavior + regress-guard flat path). **C-bug fix (plan §Open Q4)**: `REDIT_EXIT_VNUM` now updates both `pexit.Vnum` AND `pexit.ToRoom`; C at `src/oredit.c:860-875` sets vnum only, leaving ToRoom pointing at the stale prior target (silently destructive). Pinned by `TestReditParse_ExitVnum_SetsBothFields`. **G5 EditorSave wording verified correct**: the `/s` handler at `game/editor.go:163-172` transitions Connected→CON_PLAYING BEFORE invoking the stashed callback, so the `2`-branch closure re-sets CON_REDIT AFTER StopEditing (NOT overrides a live transition). Mirrors C's `do_redit_reset` at `src/oredit.c:493-535`. **Main-menu Q emits "Exiting editor."** (not "Room saved."): audit readiness-vet note applied — C `cleanup_olc` discards the session, and the no-op save-confirm block is scope-cut (plan §Scope Cuts), so the wording matches observed behavior. **ANSI screen-clear sequence omitted** per plan §Open Q1 — testclient harness cannot interpret cursor-position codes; cosmetic-only in C. **Menu has no `> ` sentinel**: E2E tests match on "Enter choice" terminator string. **9 mutation gates verified** via `Edit` round-trips only (banned-command list respected): drop DoRedit Connected assignment → `TestDoRedit_NoArgEntersMenu` fails; swap CON_PLAYING→CON_REDIT in cleanupOlc → 2 tests fail on Quit; flip tunnel URANGE upper 1000→100 → `TestReditParse_Tunnel_Clamps` fails on 9999 case; Toggle→Set in flag branch → `TestReditParse_Flags_NumberToggle` fails on 2nd toggle; drop SECT_DUNNO guard → `TestReditParse_Sector_RejectsDunno` fails; drop `pexit.ToRoom = target` Go-bug-fix in REDIT_EXIT_VNUM → `TestReditParse_ExitVnum_SetsBothFields` fails; drop extradesc empty-junk branch → `TestReditParse_Extradesc_AddKeyQuitJunksIfEmpty` fails; swap CON_REDIT→CON_OEDIT in loop.go arm → `TestTestclient_ReditMenuEntryAndQuit` E2E fails with "Unexpected state"; drop CON_REDIT re-set in EditorSave closure → `TestReditParse_DescEditorCallback_ReturnsToConRedit` fails. **Scope cuts preserved** (plan §Scope Cuts): REDIT_CONFIRM_SAVESTRING flow (constant reserved for future), `redit_setup_new` (partial Phase 4b `DoRdig`), double-edit guard (cleaner once oedit/medit generalize the lookup), `OLC_CHANGE` dirty-bit (allocated, no consumer), persisted build-log, fine-grained `can_rmodify` permission model. 43 new tests total. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean on all changed/new files (pre-existing unformatted files in tree are unchanged), `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. |
| `plan-phase6-polymorph.md` | **LANDED 2026-04-19** | Polymorph subsystem — 7 task groups (G0 DiceParse promoted; G1 LoadMorphs/SaveMorphs/SetupMorphVnum + World.Morphs; G2 handler.DoMorph/DoUnmorph/MakeCharMorph/SendMorphMessage/CanMorph/DoMorphChar/DoUnmorphChar/UnmorphAll direct-mutation primitives; G3 immortal morph/unmorph with trust-gate; G4a+G4b morphstat/morphcreate/morphdestroy/morphset with 44-field full coverage; G5 mpMorph/mpUnmorph mudprog bodies filled in; G6 #MorphData pfile block with Vnum-before-Name ordering + defensive graceful-degradation), 20 acceptance criteria all satisfied. **Direct-mutation pattern** per plan §4.2 (not AffectModify — C-verbatim). **C bugs preserved verbatim with pin-tests**: NoImmune strip asymmetry on DoUnmorph, DoMorphChar non-atomic check-and-spend ordering, C's `Move→morph_self` typo at `polymorph.c:1916`. **Go-port divergences documented**: DoMorphcreate rejects duplicate-by-name (admin UX); `update_aris` omitted (Go incremental-AffectModify invariant); LoadPlayer trailing-section handling for #MorphData after End. **Q3/Q6/Q7 resolutions**: SeparateObj no-op + ExtractObj functional; two-phase load order (morphs AFTER classes/races/areas, BEFORE ResetAllAreas); SetupMorphVnum matches C exactly — start from `max(existing, 999)+1`. 7 mutation gates verified via `Edit` round-trips only (banned-command list respected). +68 new tests across 5 packages. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass per documented tooling caveat. Commit `b504b33`. |
| `plan-phase6-archery.md` | **LANDED 2026-04-19** | DoDraw/DoFire/DoDislodge + arrow-lodge mechanic — 6 task groups, 15 acceptance criteria all satisfied. **3 C bugs preserved verbatim with pin-tests**: (1) `do_draw` WEAR_DUAL_WIELD omission from hand-count at `src/archery.c:142-149` → `TestDoDraw_DualWieldOmittedFromHandCount_CBugPreserved`; (2) `do_dislodge` arm damage `3*v[1]..2*v[2]` asymmetry vs rib's 3/3 and leg's 2/2 at `:234` → `TestDoDislodge_ArmSuccess_PreservesAsymmetry` (deterministic val=2,3 → dam=6) + `TestDoDislodge_ArmSuccess_UpperBoundAsymmetry` (200-iter val=1,10 → dam ≤ 20 probabilistic catch); (3) `do_fire` structurally-dead `victim==ch` check on never-assigned pointer at `:1255,1272` → `TestDoFire_VictimEqualsCh_DeadCheck_CBugPreserved`. **Combat-loop hook OMITTED** (C's `one_hit` never reads WEAR_MISSILE_WIELD — archery is strictly `do_fire`-driven). **Schema bumps**: `ITEM_WEAR_MAX` `21→24` (for bits 22/23/24 = new ITEM_LODGE_*); `MAX_WEAR` `26→29` (for the 3 new slots). Verified zero external callers hard-coded the old values. **Go-port simplifications vs C** (all inline-documented): PC THAC0 simplified to `20-ch.Level` matching `combat/combat.go:491`; RIS bitmap check at C `:435-493` omitted (Go has no public `ris_damage`); weapon-spell iteration at C `:608-629` omitted (no `skill_table[sn].spell_fun` dispatch from `act/`); `learn_from_failure` on miss omitted; `can_use_skill` gate collapsed to `util.NumberPercent() > 50` because Go folds archery/blowguns/slings into `gsnMissileWeapons`. Quiver auto-reload NOT shipped (C doesn't auto-reload — preserve verbatim). `mob_fire` autonomous NPC firing deferred (stub doc only). `SeparateObj` implemented as no-op (Go has no object stacking); `isSafe` / `maxFight` are minimal local helpers (full ports tracked as `archery-followup-is-safe` / `archery-followup-max-fight` in TODO.md). 52 new tests (47 `archery_test.go` + 4 `archery_constants_test.go` + 1 boot). 6 mutation gates verified via `Edit` round-trips only. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean, `go test -count=3 ./...` green across all 15 packages. Structured self-review substituted for external adversary pass. Commit `66cc205`. |
| `plan-phase6-housing.md` | **audited 2026-04-19 (CONCERNS, 5 corrections applied in-place)** | Player housing / apartments — 11 groups (G0-G10), 18 criteria, 10 open questions. 730 doc lines. Six commands: `do_house`/`do_gohome`/`do_residence`/`do_accessories`/`do_homebuy`/`do_sellhouse`. `HOME_DATA`/`HOMEBUY_DATA`/`ACCESSORIES_DATA` struct citations corrected to `src/house.h:66-118`. Uses Tier 12 `EditorSave` callback for per-room description customization. Soft-depends on hotboot. **Audit corrections:** (1) `homebuy_update` cadence fixed from "hourly/`PULSE_TICK`" → **every 30 min** via `1800*PULSE_PER_SECOND` (`src/update.c:2687-2691`); would have been a 60× wrong tick rate. (2) `LEVEL_DEMI`/`LEVEL_GREATER` threshold roles were swapped — `LEVEL_GREATER` gates `house set/remove/givekey <name>` (`src/house.c:307`), would have been a privilege-escalation bug. (3) `WriteArea` renamed to `persist.SaveArea`. (4) Q6 pre-resolved (no executor grep). (5) Object-helper names corrected: `writePlayerObj` / `readPlayerObject` (unexported, same-package). Q3 (persist.LoadPlayer offline support) and Q9 (`gohome`-from-arena policy) remain pre-G-start human gates. |
| `plan-phase6-overland-loader.md` | **audited 2026-04-19 (PASS-with-notes, 2 corrections applied in-place)** | Overland loader + display — 13 groups (G1-G13), 17 criteria. ~820 doc lines. Raw-binary map file format (new loader shape; `persist.Scanner` is line-oriented so overland adds a sibling `persist.BinScanner`), sector-type lookup, coordinate utilities, `coords`/`survey`/`landmarks` player commands, `display_map` ANSI rendering. **Critical sector-enum migration**: Go `enums.go:549-567` has 16 `SECT_*` values in non-`OVERLANDCODE` order (AIR/UNDERWATER swapped) — Q3 is 3-option decision (translation table / version field / bulk re-save) gating G1 start. Stub `MapData` at `room.go:105-109` (`Vnum int` only) replaced with full 4-field C `map_data`. Q1 (`mapreset` missing-`break` C bug), Q2 (`ROOM_MAP` bit collision audit — pre-resolvable today), Q4 flag-presence audit queue at G1. **Audit corrections:** (1) `MAP_INDEX_DATA` "unused in stock code path" claim was wrong — `db.c:2463-2500`/`:6136` + `mapout.c:78-141` use it; rationale relabeled to "not yet exercised by Go-port code paths". (2) `ACT_SENTINEL` (`constants.go:445`) and `ACT_ONMAP` (`:488`) already defined — removed from Missing list to prevent duplicate-add. |
| `plan-phase6-overland-builder.md` | **audited 2026-04-19 (PASS-with-notes, 5 corrections applied in-place)** | Overland admin editor — 12 groups (G0-G12), 15 criteria. ~720 doc lines. G0 is a pre-flight audit of loader completion; rest ports `setmark`/`setexit`/`mapresets`/`mreset`/`mapedit` admin commands + landing-site persistence. Blocked on overland-loader landing. **Audit corrections:** (1) G0 rewritten from narrative bullets to a 7-row concrete grep checklist with binary pass criteria. (2) `DoSetmark desc` editor-seam relabeled as documented Go divergence (C uses `ch->substate`+re-entrant dispatch, Go uses `EditorSave` closure — behaviorally equivalent, seam shape differs). (3) Q1 "race" framing corrected (single-goroutine loop — actual concern is undo-list state corruption); pre-resolved to option b (`PurgeUndo` at `ReloadMap` top). (4) G7 test 8 pinned to "reject-without-assign" — C at `:3520-3536` assigns `secedit=value` before the `exit` check; Go intentionally fixes. (5) `save_map` path-traversal guard requirement + `TestSaveMapRejectsPathTraversal` added (`strings.ToLower("../../etc/passwd")` unchanged). |
| `plan-phase6-olc-medit.md` | **authored + audited 2026-04-19 (CONCERNS → corrections applied, execution-ready)** | Interactive Mob/Character Editor (`CON_MEDIT`) — 15 task groups G1-G15, 35 acceptance criteria A1-A35, 16 mutation gates M1-M16. 982 doc lines. Third of three OLC-substate plans; inherits oedit pattern with `MEDIT_* = iota+300`. Ports `src/omedit.c` (2280 LOC) and 64 MEDIT_* constants from `src/olc.h:198-263`. **Unique complexity vs oedit**: dual PC-vs-NPC main-menu arms (`MEDIT_NPC_MAIN_MENU=0` + `MEDIT_PC_MAIN_MENU=1`) dispatched on `victim.IsNPC()` — full PC/NPC/shared mode-ownership matrix documented. **G9 shared `olcBitmaskEdit` helper** (AFF_FLAGS + PLR_FLAGS + ACT_FLAGS + PCDATA_FLAGS + PARTS + RIS bits) is what oedit G9 deferred; **G14 back-wires oedit's OEDIT_AFFECT_MODIFIER placeholder** to consume it (hard deliverable, not follow-up). **G13 MEDIT_PASSWORD** is security-critical: uses `bcrypt.GenerateFromPassword` (verified in project — `golang.org/x/crypto/bcrypt`, existing callers in `internal/act/playercfg.go:260-266` + `internal/game/loop.go:415,499`), pre-hash tilde-strip on raw input (safer than C's post-hash check — documented Go divergence since bcrypt's base64 alphabet never produces `~` anyway), min-length 5, trust gate TBD at G-start (C uses `LEVEL_SUB_IMPLEM`; plan description suggests LEVEL_GREATER — resolve by reading C `:1602`). **MEDIT_CONFIRM_SAVESTRING=9 IS LIVE** in medit (`omedit.c:1011-1057`) — differs from oedit's dead-constant precedent; PC Q-exit enters this mode with Y/N save-confirm (Y→`save_char_obj` or `fold_area`; N→cleanupOlc); NPC Q skips directly to cleanupOlc (audit correction — plan initially mirrored oedit's reserved-but-unwired pattern). **Audit corrections applied in-place (8 edits + 2 categories of worker-action)**: C entry `do_mmedit`→`do_omedit` (6 sites); `medit_disp_menu` branch condition inverted; `MEDIT_PRACTICE` URange `(0,250)`→`(1,300)` per C; stat clamps `(3,25)` all→NPC `(1,25)`/PC `(3,18)` per C `:998-1007`; `OlcData.Victim` typed-field proposal dropped in favor of existing `Target any` pattern; `SavingThrow[N-1]` indexed-array→named CharData fields; M5 `util.CryptPassword`→`bcrypt.GenerateFromPassword`; NPC dispatch skeleton digit-assignment warnings added; `MEDIT_CONFIRM_SAVESTRING` reserved→LIVE. `util.IsNpc(v)`→`victim.IsNPC()` applied orchestrator-side (5 code-sketch sites). **Worker-action notes**: `util.IsNpc` references already fixed; MEDIT_CONFIRM_SAVESTRING and password-trust-gate are G-start decisions. **Blockers**: none. All 4 critical Qs resolved pre-dispatch (`DoMset` exists, bcrypt confirmed, ClassLookup/RaceLookup absent → numeric-only with TODO, D_DESC target field confirmed). Q5-Q12 are verify-at-G-start (non-blocking). **Blocks**: `plan-phase6-olc-mpedit.md` (third and final OLC editor; MPROGEDIT_* iota space will extend beyond medit). Ready to dispatch. |
| `plan-phase6-olc-oedit.md` | **LANDED 2026-04-19** | Interactive Object Editor (`CON_OEDIT`) — 13 task groups (G1-G13) delivered across 4 execution waves, all 27 acceptance criteria A1-A27 satisfied, ≥12 mutation gates verified via `Edit` round-trips only (banned-command list respected). Second of three OLC-substate plans; inherits `CON_REDIT` pattern (commit `f678e8a`) + `OlcData` iota-space convention (OEDIT_* = iota+200). Ports 37 OEDIT_* modes from `src/ooedit.c` (2160 C LOC) + 6 per-item-type value-menu dispatchers (`:623-849`) + OEDIT_AFFECT_MENU skeleton (`:1735-1931`) + full OEDIT_EXTRADESC CRUD. **Wave 1 (`6f42126`)**: G1 schema (37 OEDIT_* at `iota+200`, `SUB_OBJ_ACTION` substate, `MAX_OLC_ITEMS_LIST=61`); G2 `olcLog` target-label generalization; G3 dispatcher skeleton + `CON_OEDIT` loop arm at `internal/game/loop.go:279-283`; G11 path-containment guard at `DoSaveArea` (closes the post-Phase-6 audit future-risk finding — absolute paths rejected, `..` segments escaping `<DataDir>/area/` rejected, pinned by 4 unit tests). **Wave 2 (`578fa97`)**: G4 menu renderers in new `internal/game/oedit_menu.go` (~495 LOC — main menu with 17 options `1-9/A-G/Q`, plus type/extras/wear/layer/extradesc/value-stub sub-menus); G5 main-menu dispatch in new `internal/game/oedit_parse.go` (`Q`→cleanupOlc; `1-9`→simple prompts; `3+4`→EditorSave trampolines re-setting `Connected=CON_OEDIT` post-`/s`; `A-G`→sub-menus); G6 simple-field parse arms with SmashTilde + URange clamps + `LEVEL_GREATER` trust gate on level and ITEM_PROTOTYPE flag. **Wave 3 (`1bdbd72`)**: PRE-G D-branch layerable-wear precheck fix (C `ooedit.c:1261-1275`); G7 per-item-type value bodies in new `internal/game/oedit_values.go` (val1..val6 dispatch on `idx.ItemType`: LIGHT skips to val3, HERB skips to val2, LEVER/SWITCH→lever-flags menu, standard→typed prompt; container/lever/liquid/weapon/spells sub-helpers); G8 `OEDIT_EXTRADESC_*` CRUD in new `internal/game/oedit_extras.go` (A/R/Q + keyword-then-description editor trampoline, prototype-only per §Q3); G9 affect-menu placeholder (numeric `APPLY_*` locations only; `APPLY_EXT_AFFECT` rejected until medit's `medit_disp_aff_flags` renderer lands per §Q4). **Wave 4 (commit `TBD` — orchestrator owns)**: G10 `DoOedit` menu-entry extension — `oedit <vnum>` with no subcommand flips `Connected=CON_OEDIT`, allocates `OlcData{Mode:OEDIT_MAIN_MENU, Vnum:vnum, Target:idx}`, fires `OeditDispMenuFunc(ch.Desc)`; fully-empty `oedit` still prints the usage line (matches C's `"OEdit what?"` when `ENABLE_OLC2_EXTRAS` is off); flat `oedit <vnum> <sub> [args]` path unchanged; old no-arg-summary rebound under explicit `oedit <vnum> show` (Q2 resolution). G12 testclient E2E — 6 scenarios in new `internal/testclient/oedit_test.go` (menu-entry-and-quit, set-name, invalid-choice-redisplays, type-submenu, longdesc-`/s`-roundtrip verifying `Connected=CON_OEDIT` post-save, area-save path-traversal guard E2E). **4 C-bug divergences preserved verbatim**: OEDIT_AFFECT_RIS declared-but-unreachable dead-code path (pinned by `TestOeditParse_AffectRis_UnreachableBugs`), OEDIT_CONFIRM_SAVESTRING commented-out in main-menu `Q` (Q goes straight to cleanupOlc), OEDIT_TYPE stay-in-mode on invalid (reprompt without transition), OEDIT_VALUE_1 ITEM_MONEY missing-break (no-op in stock GSC-off build — GSC scope-cut). **C divergences documented inline**: prototype-only mutation (matches flat `DoOedit`/`DoOset` convention — ITEM_PROTOTYPE mirror-write skipped), `SUB_OBJ_ACTION` editor path is Go-port addition (C uses single-line OEDIT_ACTDESC inline input), full affect-flag/RIS bitmask editor deferred to medit-plan landing (placeholder accepts numeric `APPLY_*` locations only), `can_omodify` fine-grained permission simplified to `LEVEL_IMMORTAL` single gate, ANSI screen-clear sequences omitted per redit Q1 precedent. **Follow-ups queued in `TODO.md`**: (1) `util.SkillLookup` seam so `game/` can resolve spell names in OEDIT_VALUE_2/3/4/5/6 (currently numeric-sn-only per §Q5 Option 2 — `combat.lookupSkillSlot` is closest existing helper); (2) replace OEDIT_AFFECT_MODIFIER placeholder with full affect-flag+RIS bitmask editor post-medit; (3) implement full OEDIT_AFFECT_MENU edit-by-index helper (currently a stub per §Q11 — numeric input redisplays the menu rather than opening the edit trampoline). **Unblocks**: `plan-phase6-olc-medit.md` (will inherit pattern with MEDIT_* = iota+300) + `plan-phase6-olc-mpedit.md` (hard-blocked on oedit + medit landing). **File deltas across 4 waves**: new files total ~3900 LOC of which ~2400 are tests (~62%) — new `internal/game/oedit_menu.go` (495), `internal/game/oedit_parse.go` (554), `internal/game/oedit_values.go` (617), `internal/game/oedit_extras.go` (371), `internal/game/oedit_menu_test.go` (231), `internal/game/oedit_parse_test.go` (759), `internal/game/oedit_wave3_test.go`, `internal/testclient/oedit_test.go`, plus `internal/types/olc.go` +OEDIT_* block, `internal/types/enums.go` +SUB_OBJ_ACTION, `internal/act/olc.go` +G11 path-guard, `internal/act/olc_interactive.go` G10 extension. `go build ./...` clean, `go vet ./...` clean, `gofmt -l` clean on all changed/new files, `go test -count=3 ./...` green across all 15 packages (testclient package ~22.5s boot-per-test budget). Structured self-review substituted for external adversary pass per documented tooling caveat. |

**Remaining Phase-6 prerequisites:** none blocking Wave 1; Wave 2+ blockers covered by per-plan open questions. **17 Phase-6 plans authored; all audited as of 2026-04-19** — housing/overland-loader/overland-builder pending-pile cleared 2026-04-19 (CONCERNS/PASS-with-notes/PASS-with-notes respectively; 12 surgical corrections total applied in-place; see table rows above). **Status shifts 2026-04-19:** marriage **DEFERRED** (human decision — park, revisit on player demand signal); hotboot **LANDED 2026-04-19** (Linux/Unix only via `//go:build !windows`; Q1 resolved + full executable rewrite + 8-wave execution + integration test green same day); olc-oedit **LANDED 2026-04-19** across 4 execution waves (G1-G13 complete, 27/27 acceptance criteria, unblocks medit + mpedit). **Still unauthored:** `plan-phase6-olc-mpedit.md` (hard-blocked on medit), `plan-phase6-dragonflight.md` (hard-blocked on overland-loader), `plan-phase6-deity-prayer.md` (Wave 6; roadmap claim of ~400 C LOC is wrong — `src/deity.c` is 1695 LOC, scope narrowing needed), `plan-phase6-foldarea.md` (deferred by policy). 3 surgical plan edits applied during vet: holidays A14/A15 added (Q6 resolution), planes A1 empty-slice vs nil clarified, archery Q12/Q13 added. Manager subagent harness `Agent`-tool availability remains inconsistent — all 2026-04-18 audits/authorings fell back to structured self-review; tooling investigation tracked in TODO.md. Small follow-up items in `TODO.md` Active — deferred mudprog ifchecks (`isflagged`/`istagged`), per-player color customization, `DoBlank` toggle, `DoConfig` surface, SmashTilde-ordering decision for planes rename, `combat.Damage` self-target safety verification before bloodlet G3, Wave D items (`ITEM_WEAR_MAX` bump for archery, lodged-arrow-remove policy, mob_fire value-index asymmetry, morphset G4a/G4b split).

**Reviews:**

| File | Contents |
|---|---|
| `security-review.md` | 16 findings, 14 fixed |
| `go-idiom-review.md` | Go idiom audit: 10 findings, all resolved |
| `audit-2026-04-17.md` | Post-Phase-5 4-adversary audit: phase completeness, idiom, security/usability, C↔Go capability — verdict CONCERNS |

## Build and run

```bash
cd smaug-go
go build -o smaug-go ./cmd/smaug/
./smaug-go -port 4000 -data ../db
# connect: telnet localhost 4000
```

Module: `github.com/eilidhmae/smaug`. Build all: `go build ./...`. Test all: `go test ./...`.

On fresh clone: `make install-hooks` (from `smaug-go/`) activates the pre-commit gate (gofmt + vet + unit tests + conflict markers). Bypass with `--no-verify`; skip individual gates with `SMAUG_SKIP_VET=1` / `SMAUG_SKIP_TESTS=1`.

## Conventions (not covered in `plan.md`)

- C enums → untyped `int` constants (not typed Go enums) so they interop cleanly with struct fields
- Struct fields use PascalCase; constant names match C exactly for cross-reference (`ACT_IS_NPC`, `PULSE_VIOLENCE`, `ROOM_VNUM_TEMPLE`)
- `types/` has no logic dependencies — everything else may import it
- `world.World` is passed explicitly, never a global. `act.WorldRef` is a package-level set-at-boot exception for commands.
- File loaders: `util.Bug()` and continue on bad data (don't abort — matches C behavior)
- No Cgo. Use pure Go / stdlib equivalents (e.g. `compress/zlib` for MCCP)

## TDD (mandatory)

`plan.md`'s Testing Strategy section points here for workflow details.

1. Write failing test → confirm red
2. Write minimum code to pass → confirm green
3. Refactor → confirm still green
4. Commit

For existing code, backfill tests and verify with **mutation testing**: stage working code (`git add`), mutate the implementation (flip a condition / change a return), run tests → must fail, revert (`git checkout`), run tests → must pass.

Tests live next to code (`foo.go` → `foo_test.go`). Fixtures go in `testdata/`. Aim for high coverage on new/modified code: `go test -cover ./...`.
